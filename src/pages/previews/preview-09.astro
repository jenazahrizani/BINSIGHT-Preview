---
import {
  ArrowRight,
  ArrowUpRight,
  Building2,
  ChartColumn,
  FileText,
  Map,
  Menu,
  Recycle,
  ShieldCheck,
  X,
} from "@lucide/astro";

const BASE_URL = (import.meta.env.BASE_URL || "/").replace(/\/$/, "");

const siteHref = (path: string) => {
  if (
    path.startsWith("http://") ||
    path.startsWith("https://") ||
    path.startsWith("mailto:") ||
    path.startsWith("tel:") ||
    path.startsWith("#") ||
    path.startsWith("data:") ||
    path.startsWith("javascript:")
  ) {
    return path;
  }

  if (path === "/") {
    return `${BASE_URL}/`;
  }

  return `${BASE_URL}${path.startsWith("/") ? path : `/${path}`}`;
};

const services = [
  {
    no: "01",
    title: "Peta Persampahan",
    short: "PETA",
    text: "Jelajahi informasi persampahan berdasarkan lokasi dan wilayah di Kabupaten Badung.",
    href: "/map",
    icon: Map,
  },
  {
    no: "02",
    title: "Data & Informasi",
    short: "DATA",
    text: "Temukan informasi dalam bentuk yang lebih terstruktur, ringkas, dan mudah dibaca.",
    href: "/dashboard",
    icon: ChartColumn,
  },
  {
    no: "03",
    title: "Jaringan Organisasi",
    short: "NETWORK",
    text: "Kenali organisasi dan pihak yang membentuk ekosistem persampahan Badung.",
    href: "/organizations",
    icon: Building2,
  },
  {
    no: "04",
    title: "Material & Edukasi",
    short: "MATERIAL",
    text: "Pelajari kategori material dan pahami karakter penanganannya.",
    href: "/waste",
    icon: Recycle,
  },
];

const areas = [
  {
    no: "01",
    name: "Kabupaten Badung",
    type: "KABUPATEN",
    detail: "Wilayah utama",
    status: "TERSEDIA",
  },
  {
    no: "02",
    name: "Mengwi",
    type: "KECAMATAN",
    detail: "Area contoh",
    status: "CONTOH",
  },
  {
    no: "03",
    name: "Abiansemal",
    type: "KECAMATAN",
    detail: "Area contoh",
    status: "CONTOH",
  },
];

const materials = [
  {
    no: "01",
    code: "ORG",
    name: "Organik",
    description: "Material yang berasal dari sisa makhluk hidup.",
  },
  {
    no: "02",
    code: "ANR",
    name: "Anorganik",
    description: "Material non-organik dengan karakter yang beragam.",
  },
  {
    no: "03",
    code: "B3",
    name: "Bahan Berbahaya",
    description: "Material yang memerlukan perhatian dan penanganan khusus.",
  },
  {
    no: "04",
    code: "ELK",
    name: "Elektronik",
    description: "Perangkat dan komponen elektronik yang sudah tidak digunakan.",
  },
  {
    no: "05",
    code: "RES",
    name: "Residu",
    description: "Material tersisa yang tidak masuk kategori lainnya.",
  },
];

const currentYear = new Date().getFullYear();
---

<!doctype html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />

    <title>BINSIGHT â€” Badung Waste Intelligence</title>

    <meta
      name="description"
      content="Platform informasi persampahan Kabupaten Badung untuk menemukan, memahami, dan melaporkan kondisi persampahan."
    />

    <meta
      name="theme-color"
      content="#f5f6f1"
    />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />

    <link
      href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Inter:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <div class="site">

      <!-- =====================================================
           HEADER
      ====================================================== -->

      <header class="header">
        <div class="page-width header__inner">

          <a
            href={siteHref("/")}
            class="brand"
            aria-label="BINSIGHT"
          >
            <span
              class="brand__mark"
              aria-hidden="true"
            >
              <span></span>
              <span></span>
              <span></span>
              <span></span>
            </span>

            <span class="brand__name">
              BINSIGHT
            </span>
          </a>

          <nav
            class="desktop-nav"
            aria-label="Navigasi utama"
          >
            <a
              href={siteHref("/")}
              class="desktop-nav__active"
            >
              Beranda
            </a>

            <a href={siteHref("/map")}>
              Peta
            </a>

            <a href={siteHref("/dashboard")}>
              Data
            </a>

            <a href={siteHref("/organizations")}>
              Organisasi
            </a>

            <a href={siteHref("/waste")}>
              Material
            </a>

            <a href="#tentang">
              Tentang
            </a>
          </nav>

          <div class="header__actions">

            <span class="header__region">
              BADUNG / BALI
            </span>

            <a
              class="header__report"
              href={siteHref("/reports/create")}
            >
              Laporan
              <ArrowUpRight
                size={15}
                stroke-width={1.8}
              />
            </a>

            <button
              type="button"
              class="menu-button"
              aria-label="Buka menu"
              aria-expanded="false"
              aria-controls="mobile-nav"
            >
              <Menu
                data-menu-open
                size={21}
                stroke-width={1.8}
              />

              <X
                data-menu-close
                size={21}
                stroke-width={1.8}
              />
            </button>

          </div>

        </div>

        <div
          class="mobile-nav"
          id="mobile-nav"
        >
          <div class="page-width mobile-nav__inner">

            <div class="mobile-nav__label">
              MENU
            </div>

            <a href={siteHref("/")}>
              <span>01</span>
              Beranda
            </a>

            <a href={siteHref("/map")}>
              <span>02</span>
              Peta
            </a>

            <a href={siteHref("/dashboard")}>
              <span>03</span>
              Data
            </a>

            <a href={siteHref("/organizations")}>
              <span>04</span>
              Organisasi
            </a>

            <a href={siteHref("/waste")}>
              <span>05</span>
              Material
            </a>

            <a href="#tentang">
              <span>06</span>
              Tentang
            </a>

          </div>
        </div>
      </header>


      <main>

        <!-- ===================================================
             HERO
        ==================================================== -->

        <section class="hero">
          <div class="page-width">

            <div class="hero__topline">
              <span>PUBLIC INFORMATION PLATFORM</span>
              <span>DENPASAR REGION / 2026</span>
            </div>

            <div class="hero__grid">

              <div class="hero__copy">

                <div class="hero__index">
                  <span>01</span>
                  <span>INTRODUCTION</span>
                </div>

                <p class="hero__eyebrow">
                  BADUNG WASTE INTELLIGENCE
                </p>

                <h1>
                  Memahami
                  <span>persampahan</span>
                  dimulai dari
                  <strong>konteks.</strong>
                </h1>

                <p class="hero__description">
                  BINSIGHT adalah ruang informasi publik yang
                  menghubungkan lokasi, data, organisasi, material,
                  edukasi, dan laporan persampahan Kabupaten Badung.
                </p>

                <div class="hero__buttons">

                  <a
                    class="button button--primary"
                    href={siteHref("/map")}
                  >
                    Jelajahi peta
                    <ArrowRight
                      size={17}
                      stroke-width={1.8}
                    />
                  </a>

                  <a
                    class="button button--secondary"
                    href={siteHref("/dashboard")}
                  >
                    Buka data
                    <ArrowUpRight
                      size={17}
                      stroke-width={1.8}
                    />
                  </a>

                </div>

              </div>


              <div
                class="hero__visual"
                aria-label="Visual konteks wilayah BINSIGHT"
              >

                <div class="visual__header">
                  <span>FIELD VIEW</span>
                  <span>01 / 04</span>
                </div>

                <div class="visual__canvas">

                  <div class="map-grid"></div>

                  <div class="map-shape map-shape--a"></div>
                  <div class="map-shape map-shape--b"></div>
                  <div class="map-shape map-shape--c"></div>

                  <div class="map-route"></div>

                  <div class="map-point map-point--one">
                    <i></i>
                    <span>01</span>
                  </div>

                  <div class="map-point map-point--two">
                    <i></i>
                    <span>02</span>
                  </div>

                  <div class="map-point map-point--three">
                    <i></i>
                    <span>03</span>
                  </div>

                  <div class="visual__center">
                    <small>REGION</small>
                    <strong>BADUNG</strong>
                    <span>BALI</span>
                  </div>

                  <div class="visual__compass">
                    <span>N</span>
                    <span>+</span>
                    <span>E</span>
                  </div>

                </div>

                <div class="visual__footer">

                  <div>
                    <span class="mono-label">CONTEXT</span>
                    <strong>MAP / DATA / PEOPLE</strong>
                  </div>

                  <span class="status-dot"></span>

                </div>

              </div>

            </div>

            <div class="hero__rail">

              <div class="hero__rail-item">
                <span>01</span>
                <small>LOCATION</small>
              </div>

              <div class="hero__rail-item">
                <span>02</span>
                <small>INFORMATION</small>
              </div>

              <div class="hero__rail-item">
                <span>03</span>
                <small>NETWORK</small>
              </div>

              <div class="hero__rail-item">
                <span>04</span>
                <small>REPORT</small>
              </div>

            </div>

          </div>
        </section>


        <!-- ===================================================
             INTRO STRIP
        ==================================================== -->

        <section class="signal">
          <div class="page-width signal__grid">

            <div class="signal__intro">
              <span class="section-kicker">
                02 / WHY BINSIGHT
              </span>

              <h2>
                Satu isu.
                <span>Banyak konteks.</span>
              </h2>
            </div>

            <div class="signal__cards">

              <article class="signal-card">
                <span>01</span>

                <div>
                  <strong>
                    Lokasi
                  </strong>

                  <p>
                    Informasi lebih bermakna ketika
                    ditempatkan pada ruang yang tepat.
                  </p>
                </div>
              </article>

              <article class="signal-card">
                <span>02</span>

                <div>
                  <strong>
                    Data
                  </strong>

                  <p>
                    Informasi disusun agar lebih mudah
                    dibaca, ditemukan, dan dibandingkan.
                  </p>
                </div>
              </article>

              <article class="signal-card">
                <span>03</span>

                <div>
                  <strong>
                    Manusia
                  </strong>

                  <p>
                    Organisasi dan masyarakat merupakan
                    bagian dari ekosistem persampahan.
                  </p>
                </div>
              </article>

            </div>

          </div>
        </section>


        <!-- ===================================================
             SERVICES
        ==================================================== -->

        <section class="explore">
          <div class="page-width">

            <div class="section-header">

              <div>
                <span class="section-kicker">
                  03 / EXPLORE
                </span>

                <h2>
                  Empat ruang
                  <span>untuk dijelajahi.</span>
                </h2>
              </div>

              <p>
                BINSIGHT dirancang untuk membantu pengguna
                berpindah dari satu konteks informasi ke konteks
                lainnya tanpa kehilangan gambaran besarnya.
              </p>

            </div>


            <div class="service-grid">

              {
                services.map((service) => {
                  const Icon = service.icon;

                  return (
                    <a
                      href={siteHref(service.href)}
                      class={`service-card service-card--${service.no}`}
                    >

                      <div class="service-card__top">
                        <span>{service.no}</span>

                        <span class="service-card__type">
                          {service.short}
                        </span>
                      </div>

                      <div class="service-card__icon">
                        <Icon
                          size={27}
                          stroke-width={1.6}
                        />
                      </div>

                      <div class="service-card__body">

                        <h3>
                          {service.title}
                        </h3>

                        <p>
                          {service.text}
                        </p>

                      </div>

                      <div class="service-card__bottom">
                        <span>
                          BUKA MODUL
                        </span>

                        <ArrowUpRight
                          size={18}
                          stroke-width={1.6}
                        />
                      </div>

                    </a>
                  );
                })
              }

            </div>

          </div>
        </section>


        <!-- ===================================================
             CONTEXT
        ==================================================== -->

        <section class="context">
          <div class="page-width context__grid">

            <div class="context__side">

              <span class="section-kicker section-kicker--light">
                04 / THE CONTEXT
              </span>

              <span class="context__side-number">
                04
              </span>

              <div class="context__side-line"></div>

              <small>
                BADUNG<br />
                BALI<br />
                INDONESIA
              </small>

            </div>


            <div class="context__content">

              <div class="context__heading">
                <p class="context__lead">
                  Persampahan bukan persoalan yang berdiri sendiri.
                </p>

                <h2>
                  Ia bergerak
                  <span>di antara tempat,</span>
                  manusia,
                  <span>dan material.</span>
                </h2>
              </div>

              <div class="context__bottom">

                <p>
                  Karena itu, BINSIGHT tidak hanya menyajikan
                  satu jenis informasi. Platform ini menyusun
                  berbagai konteks agar pengguna dapat melihat
                  hubungan di antara informasi yang tersedia.
                </p>

                <div class="context__formula">
                  <span>LOCATION</span>
                  <i>+</i>
                  <span>DATA</span>
                  <i>+</i>
                  <span>NETWORK</span>
                  <i>+</i>
                  <span>MATERIAL</span>
                </div>

              </div>

            </div>

          </div>
        </section>


        <!-- ===================================================
             METHOD
        ==================================================== -->

        <section class="method">
          <div class="page-width">

            <div class="method__header">

              <div>
                <span class="section-kicker">
                  05 / HOW IT WORKS
                </span>

                <h2>
                  Dari pertanyaan
                  <span>menjadi pemahaman.</span>
                </h2>
              </div>

              <p>
                Pengalaman BINSIGHT dibuat sesederhana mungkin:
                temukan informasi, pahami konteksnya, lalu
                hubungkan dengan informasi lainnya.
              </p>

            </div>


            <div class="timeline">

              <article class="timeline__item">

                <div class="timeline__index">
                  01
                </div>

                <div class="timeline__marker">
                  <span></span>
                </div>

                <div class="timeline__copy">
                  <small>LANGKAH PERTAMA</small>

                  <h3>
                    Temukan
                  </h3>

                  <p>
                    Mulai dari lokasi, wilayah, data,
                    organisasi, atau kategori material
                    yang ingin Anda ketahui.
                  </p>
                </div>

              </article>


              <article class="timeline__item">

                <div class="timeline__index">
                  02
                </div>

                <div class="timeline__marker">
                  <span></span>
                </div>

                <div class="timeline__copy">
                  <small>LANGKAH KEDUA</small>

                  <h3>
                    Pahami
                  </h3>

                  <p>
                    Baca informasi yang tersedia dan
                    lihat konteks yang berada di baliknya.
                  </p>
                </div>

              </article>


              <article class="timeline__item">

                <div class="timeline__index">
                  03
                </div>

                <div class="timeline__marker">
                  <span></span>
                </div>

                <div class="timeline__copy">
                  <small>LANGKAH KETIGA</small>

                  <h3>
                    Hubungkan
                  </h3>

                  <p>
                    Kaitkan informasi dengan wilayah,
                    material, organisasi, dan pengalaman
                    masyarakat.
                  </p>
                </div>

              </article>

            </div>

          </div>
        </section>


        <!-- ===================================================
             AREAS
        ==================================================== -->

        <section class="areas">
          <div class="page-width">

            <div class="areas__header">

              <div>
                <span class="section-kicker">
                  06 / COVERAGE
                </span>

                <h2>
                  Badung
                  <span>dalam konteks.</span>
                </h2>
              </div>

              <p>
                Jelajahi informasi berdasarkan wilayah untuk
                mendapatkan gambaran yang lebih dekat dengan
                kondisi lokal.
              </p>

            </div>


            <div class="areas__list">

              {
                areas.map((area) => (
                  <a
                    class="area-card"
                    href={siteHref("/map")}
                  >

                    <div class="area-card__number">
                      {area.no}
                    </div>

                    <div class="area-card__main">

                      <span>
                        {area.type}
                      </span>

                      <h3>
                        {area.name}
                      </h3>

                      <p>
                        {area.detail}
                      </p>

                    </div>

                    <div class="area-card__status">
                      <span>
                        {area.status}
                      </span>

                      <ArrowUpRight
                        size={18}
                        stroke-width={1.6}
                      />
                    </div>

                  </a>
                ))
              }

            </div>

          </div>
        </section>


        <!-- ===================================================
             MATERIALS
        ==================================================== -->

        <section class="materials">
          <div class="page-width">

            <div class="materials__header">

              <div class="materials__title">
                <span class="section-kicker">
                  07 / MATERIAL
                </span>

                <h2>
                  Setiap material
                  <span>punya konteksnya.</span>
                </h2>
              </div>

              <div class="materials__description">

                <p>
                  Mengenal kategori material membantu memahami
                  bahwa setiap jenis sampah memiliki karakter
                  dan kebutuhan penanganan yang berbeda.
                </p>

                <a
                  href={siteHref("/waste")}
                  class="text-link"
                >
                  Lihat seluruh kategori
                  <ArrowRight
                    size={16}
                    stroke-width={1.8}
                  />
                </a>

              </div>

            </div>


            <div class="material-list">

              {
                materials.map((material) => (
                  <a
                    class="material-item"
                    href={siteHref("/waste")}
                  >

                    <div class="material-item__no">
                      {material.no}
                    </div>

                    <div class="material-item__code">
                      {material.code}
                    </div>

                    <div class="material-item__main">
                      <h3>
                        {material.name}
                      </h3>

                      <p>
                        {material.description}
                      </p>
                    </div>

                    <div class="material-item__arrow">
                      <ArrowUpRight
                        size={19}
                        stroke-width={1.6}
                      />
                    </div>

                  </a>
                ))
              }

            </div>

          </div>
        </section>


        <!-- ===================================================
             ABOUT / PRINCIPLES
        ==================================================== -->

        <section
          class="about"
          id="tentang"
        >
          <div class="page-width">

            <div class="about__header">

              <div>
                <span class="section-kicker">
                  08 / ABOUT BINSIGHT
                </span>

                <h2>
                  Dibuat untuk
                  <span>informasi publik.</span>
                </h2>
              </div>

              <div class="about__badge">
                <ShieldCheck
                  size={19}
                  stroke-width={1.7}
                />

                <span>
                  PUBLIC INFORMATION
                </span>
              </div>

            </div>


            <div class="about__grid">

              <div class="about__statement">

                <p class="about__number">
                  01
                </p>

                <blockquote>
                  â€œInformasi yang baik bukan hanya
                  mudah ditemukan, tetapi juga
                  mudah dipahami.â€
                </blockquote>

              </div>


              <div class="about__principles">

                <article>
                  <span>01</span>

                  <div>
                    <h3>
                      Kontekstual
                    </h3>

                    <p>
                      Informasi ditempatkan bersama
                      lokasi dan konteks yang relevan.
                    </p>
                  </div>
                </article>

                <article>
                  <span>02</span>

                  <div>
                    <h3>
                      Terbuka
                    </h3>

                    <p>
                      Informasi publik disusun agar
                      mudah ditemukan dan dipahami.
                    </p>
                  </div>
                </article>

                <article>
                  <span>03</span>

                  <div>
                    <h3>
                      Bertanggung jawab
                    </h3>

                    <p>
                      Penyajian memperhatikan sumber,
                      konteks, dan cara penggunaannya.
                    </p>
                  </div>
                </article>

              </div>

            </div>

          </div>
        </section>


        <!-- ===================================================
             CTA
        ==================================================== -->

        <section class="cta">
          <div class="page-width cta__inner">

            <div class="cta__label">
              <span>09</span>
              <span>BINSIGHT / NEXT</span>
            </div>

            <div class="cta__content">

              <div>
                <p>
                  Mulai dari apa yang
                  ingin Anda ketahui.
                </p>

                <h2>
                  Kenali.
                  <span>Pahami.</span>
                  Jaga Badung.
                </h2>
              </div>

              <a
                class="cta__button"
                href={siteHref("/map")}
              >
                Jelajahi BINSIGHT
                <ArrowUpRight
                  size={20}
                  stroke-width={1.7}
                />
              </a>

            </div>

          </div>
        </section>

      </main>


      <!-- =====================================================
           FOOTER
      ====================================================== -->

      <footer class="footer">

        <div class="page-width footer__top">

          <div class="footer__identity">

            <a
              href={siteHref("/")}
              class="brand brand--footer"
            >
              <span
                class="brand__mark"
                aria-hidden="true"
              >
                <span></span>
                <span></span>
                <span></span>
                <span></span>
              </span>

              <span class="brand__name">
                BINSIGHT
              </span>
            </a>

            <p>
              Badung Waste Intelligence.
              Platform informasi publik
              persampahan Kabupaten Badung.
            </p>

          </div>


          <div class="footer__column">

            <span>
              PLATFORM
            </span>

            <a href={siteHref("/map")}>
              Peta
            </a>

            <a href={siteHref("/dashboard")}>
              Data
            </a>

            <a href={siteHref("/organizations")}>
              Organisasi
            </a>

            <a href={siteHref("/waste")}>
              Material
            </a>

          </div>


          <div class="footer__column">

            <span>
              INFORMASI
            </span>

            <a href="#tentang">
              Tentang BINSIGHT
            </a>

            <a href={siteHref("/reports/create")}>
              Laporan masyarakat
            </a>

            <a href={siteHref("/map")}>
              Eksplorasi
            </a>

          </div>

        </div>


        <div class="page-width footer__bottom">

          <span>
            Â© {currentYear} BINSIGHT
          </span>

          <span>
            BADUNG / BALI
          </span>

          <span>
            PUBLIC INFORMATION PLATFORM
          </span>

        </div>

      </footer>


      <!-- =====================================================
           FLOATING REPORT
      ====================================================== -->

      <a
        class="floating-report"
        href={siteHref("/reports/create")}
        aria-label="Buat laporan masyarakat"
      >

        <span class="floating-report__icon">
          <FileText
            size={18}
            stroke-width={1.8}
          />
        </span>

        <span class="floating-report__copy">
          <small>
            LAPORAN MASYARAKAT
          </small>

          <strong>
            Buat laporan
          </strong>
        </span>

        <ArrowUpRight
          size={16}
          stroke-width={1.7}
        />

      </a>

    </div>


    <!-- =======================================================
         MENU SCRIPT
    ======================================================== -->

    <script>
      const menuButton =
        document.querySelector<HTMLButtonElement>(".menu-button");

      const mobileNav =
        document.querySelector<HTMLElement>(".mobile-nav");

      const menuOpen =
        document.querySelector<SVGElement>("[data-menu-open]");

      const menuClose =
        document.querySelector<SVGElement>("[data-menu-close]");

      const updateMenu = (open: boolean) => {
        if (!menuButton || !mobileNav) {
          return;
        }

        menuButton.setAttribute(
          "aria-expanded",
          String(open)
        );

        menuButton.setAttribute(
          "aria-label",
          open ? "Tutup menu" : "Buka menu"
        );

        mobileNav.classList.toggle(
          "mobile-nav--open",
          open
        );

        if (menuOpen) {
          menuOpen.style.display = open
            ? "none"
            : "block";
        }

        if (menuClose) {
          menuClose.style.display = open
            ? "block"
            : "none";
        }
      };

      updateMenu(false);

      menuButton?.addEventListener("click", () => {
        const current =
          menuButton.getAttribute("aria-expanded") === "true";

        updateMenu(!current);
      });

      document
        .querySelectorAll<HTMLAnchorElement>(".mobile-nav a")
        .forEach((link) => {
          link.addEventListener("click", () => {
            updateMenu(false);
          });
        });

      window.addEventListener("resize", () => {
        if (window.innerWidth > 860) {
          updateMenu(false);
        }
      });
    </script>


    <style>
      :root {
        --bg: #f5f6f1;
        --bg-soft: #eceee7;
        --surface: #ffffff;

        --ink: #101820;
        --ink-soft: #4f5960;
        --muted: #7a8287;
        --line: #d6dad4;

        --navy: #10232f;
        --navy-2: #183443;

        --lime: #c7ee45;
        --lime-soft: #e6f6ae;

        --blue: #4f7cff;

        --page: 1280px;
      }


      /* =====================================================
         RESET
      ====================================================== */

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        margin: 0;

        background: var(--bg);

        color: var(--ink);

        font-family: "Inter", sans-serif;

        -webkit-font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
      }

      a {
        color: inherit;
        text-decoration: none;
      }

      button {
        font: inherit;
      }

      svg {
        flex-shrink: 0;
      }

      .site {
        min-height: 100vh;
        overflow: clip;
      }

      .page-width {
        width: min(
          calc(100% - 64px),
          var(--page)
        );

        margin-inline: auto;
      }


      /* =====================================================
         HEADER
      ====================================================== */

      .header {
        position: sticky;

        top: 0;

        z-index: 1000;

        background:
          rgba(245, 246, 241, 0.9);

        backdrop-filter: blur(18px);

        border-bottom:
          1px solid
          rgba(16, 24, 32, 0.1);
      }

      .header__inner {
        min-height: 74px;

        display: flex;

        align-items: center;

        gap: 28px;
      }

      .brand {
        display: inline-flex;

        align-items: center;

        gap: 11px;

        flex-shrink: 0;
      }

      .brand__mark {
        width: 25px;
        height: 25px;

        display: grid;

        grid-template-columns:
          repeat(2, 1fr);

        grid-template-rows:
          repeat(2, 1fr);

        gap: 2px;
      }

      .brand__mark span {
        background: var(--ink);
      }

      .brand__mark span:nth-child(2) {
        background: var(--lime);
      }

      .brand__mark span:nth-child(3) {
        background: var(--blue);
      }

      .brand__mark span:nth-child(4) {
        background: var(--ink);
      }

      .brand__name {
        font-family: "Space Grotesk", sans-serif;

        font-size: 15px;

        font-weight: 700;

        line-height: 1;

        letter-spacing: 0.02em;
      }

      .desktop-nav {
        display: flex;

        align-items: center;

        gap: 25px;

        margin-left: auto;
        margin-right: auto;
      }

      .desktop-nav a {
        position: relative;

        color: #677077;

        font-size: 11px;

        font-weight: 600;

        transition:
          color 160ms ease;
      }

      .desktop-nav a:hover {
        color: var(--ink);
      }

      .desktop-nav__active {
        color: var(--ink) !important;
      }

      .desktop-nav__active::before {
        content: "";

        position: absolute;

        width: 5px;
        height: 5px;

        left: 50%;

        bottom: -12px;

        transform:
          translateX(-50%);

        border-radius: 999px;

        background: var(--lime);
      }

      .header__actions {
        display: flex;

        align-items: center;

        gap: 16px;
      }

      .header__region {
        color: #7d8589;

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;

        white-space: nowrap;
      }

      .header__report {
        min-height: 38px;

        padding: 0 13px;

        display: inline-flex;

        align-items: center;
        justify-content: center;

        gap: 8px;

        background: var(--ink);

        color: white;

        font-size: 10px;

        font-weight: 700;

        transition:
          background 160ms ease;
      }

      .header__report:hover {
        background: var(--navy-2);
      }

      .menu-button {
        width: 40px;
        height: 40px;

        display: none;

        align-items: center;
        justify-content: center;

        padding: 0;

        border:
          1px solid
          var(--line);

        background: transparent;

        color: var(--ink);

        cursor: pointer;
      }

      .menu-button [data-menu-close] {
        display: none;
      }

      .mobile-nav {
        display: none;

        border-top:
          1px solid
          var(--line);
      }

      .mobile-nav__inner {
        padding: 13px 0 18px;
      }

      .mobile-nav__label {
        padding: 5px 0 12px;

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .mobile-nav a {
        min-height: 54px;

        display: grid;

        grid-template-columns: 34px 1fr;

        align-items: center;

        border-bottom:
          1px solid
          var(--line);

        font-size: 14px;

        font-weight: 700;
      }

      .mobile-nav a span {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }


      /* =====================================================
         SHARED
      ====================================================== */

      .section-kicker {
        display: inline-block;

        color: #717a7f;

        font-family: "DM Mono", monospace;

        font-size: 8px;

        line-height: 1;

        letter-spacing: 0.1em;
      }

      .section-kicker--light {
        color:
          rgba(255, 255, 255, 0.52);
      }


      /* =====================================================
         HERO
      ====================================================== */

      .hero {
        background: var(--bg);
      }

      .hero__topline {
        min-height: 46px;

        display: flex;

        align-items: center;
        justify-content: space-between;

        border-bottom:
          1px solid
          var(--line);

        color: #788084;

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .hero__grid {
        min-height: 690px;

        display: grid;

        grid-template-columns:
          minmax(0, 1.08fr)
          minmax(400px, 0.92fr);

        gap: 70px;

        align-items: center;
      }

      .hero__copy {
        padding: 75px 0;
      }

      .hero__index {
        margin-bottom: 30px;

        display: flex;

        align-items: center;

        gap: 14px;
      }

      .hero__index span:first-child {
        width: 27px;
        height: 27px;

        display: grid;

        place-items: center;

        border:
          1px solid
          var(--ink);

        border-radius: 50%;

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .hero__index span:last-child {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.09em;
      }

      .hero__eyebrow {
        margin: 0 0 22px;

        color: var(--navy);

        font-family: "DM Mono", monospace;

        font-size: 9px;

        letter-spacing: 0.09em;
      }

      .hero h1 {
        max-width: 750px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          58px,
          7.4vw,
          102px
        );

        line-height: 0.92;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .hero h1 span {
        display: block;

        color: var(--navy);
      }

      .hero h1 strong {
        display: block;

        color: var(--blue);

        font-weight: 600;
      }

      .hero__description {
        max-width: 520px;

        margin: 38px 0 0;

        color: var(--ink-soft);

        font-size: 15px;

        line-height: 1.8;
      }

      .hero__buttons {
        margin-top: 34px;

        display: flex;

        flex-wrap: wrap;

        gap: 10px;
      }

      .button {
        min-height: 50px;

        padding: 0 18px;

        display: inline-flex;

        align-items: center;
        justify-content: center;

        gap: 10px;

        font-size: 10px;

        font-weight: 700;

        transition:
          transform 160ms ease,
          background 160ms ease,
          border-color 160ms ease;
      }

      .button:hover {
        transform: translateY(-2px);
      }

      .button--primary {
        background: var(--ink);

        color: white;
      }

      .button--primary:hover {
        background: var(--navy);
      }

      .button--secondary {
        border:
          1px solid
          var(--line);

        background: transparent;

        color: var(--ink);
      }

      .button--secondary:hover {
        border-color: var(--ink);
      }

      .hero__visual {
        position: relative;

        width: 100%;

        background: var(--navy);

        color: white;
      }

      .visual__header,
      .visual__footer {
        min-height: 47px;

        padding: 0 20px;

        display: flex;

        align-items: center;
        justify-content: space-between;

        border-bottom:
          1px solid
          rgba(255, 255, 255, 0.12);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .visual__header {
        color:
          rgba(255, 255, 255, 0.46);
      }

      .visual__canvas {
        position: relative;

        min-height: 500px;

        overflow: hidden;

        background:
          #122834;
      }

      .map-grid {
        position: absolute;

        inset: 0;

        opacity: 0.28;

        background-image:
          linear-gradient(
            rgba(255,255,255,0.06) 1px,
            transparent 1px
          ),
          linear-gradient(
            90deg,
            rgba(255,255,255,0.06) 1px,
            transparent 1px
          );

        background-size:
          38px 38px;
      }

      .map-shape {
        position: absolute;

        border:
          1px solid
          rgba(199, 238, 69, 0.35);

        background:
          rgba(199, 238, 69, 0.035);

        transform: rotate(-16deg);
      }

      .map-shape--a {
        width: 290px;
        height: 390px;

        left: 85px;
        top: 55px;

        border-radius:
          50% 38% 56% 42%;
      }

      .map-shape--b {
        width: 210px;
        height: 250px;

        right: 45px;
        top: 110px;

        border-radius:
          42% 58% 38% 62%;

        transform:
          rotate(22deg);
      }

      .map-shape--c {
        width: 170px;
        height: 190px;

        left: 210px;
        bottom: 22px;

        border-radius:
          56% 44% 60% 40%;

        transform:
          rotate(29deg);
      }

      .map-route {
        position: absolute;

        left: 72px;
        right: 55px;
        top: 52%;

        height: 2px;

        background:
          rgba(255, 255, 255, 0.14);

        transform: rotate(-14deg);
      }

      .map-route::after {
        content: "";

        position: absolute;

        left: 0;
        top: 0;

        width: 44%;

        height: 2px;

        background: var(--lime);
      }

      .map-point {
        position: absolute;

        z-index: 2;

        display: flex;

        flex-direction: column;

        align-items: center;

        gap: 5px;

        font-family: "DM Mono", monospace;

        font-size: 7px;
      }

      .map-point i {
        width: 13px;
        height: 13px;

        display: block;

        border:
          3px solid
          var(--navy);

        border-radius: 50%;

        background: var(--lime);

        box-shadow:
          0 0 0 1px
          rgba(199, 238, 69, 0.5);
      }

      .map-point--one {
        top: 25%;
        left: 31%;
      }

      .map-point--two {
        top: 48%;
        right: 24%;
      }

      .map-point--three {
        bottom: 18%;
        left: 48%;
      }

      .visual__center {
        position: absolute;

        z-index: 3;

        left: 50%;
        top: 50%;

        transform:
          translate(-50%, -50%);

        display: flex;

        flex-direction: column;

        align-items: center;

        text-align: center;
      }

      .visual__center small {
        margin-bottom: 7px;

        color:
          rgba(255, 255, 255, 0.48);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.14em;
      }

      .visual__center strong {
        font-family: "Space Grotesk", sans-serif;

        font-size: 47px;

        line-height: 0.9;

        letter-spacing: -0.06em;
      }

      .visual__center span {
        margin-top: 9px;

        color: var(--lime);

        font-family: "DM Mono", monospace;

        font-size: 8px;

        letter-spacing: 0.12em;
      }

      .visual__compass {
        position: absolute;

        top: 24px;
        right: 22px;

        width: 42px;
        height: 42px;

        display: grid;

        place-items: center;

        border:
          1px solid
          rgba(255, 255, 255, 0.16);

        color:
          rgba(255, 255, 255, 0.5);

        font-family: "DM Mono", monospace;

        font-size: 6px;
      }

      .visual__compass span:nth-child(2) {
        color: var(--lime);
      }

      .visual__footer {
        border-top:
          1px solid
          rgba(255, 255, 255, 0.12);

        border-bottom: 0;

        color:
          rgba(255, 255, 255, 0.5);
      }

      .visual__footer > div {
        display: flex;

        align-items: center;

        gap: 10px;
      }

      .mono-label {
        color:
          rgba(255, 255, 255, 0.34);
      }

      .visual__footer strong {
        color:
          rgba(255, 255, 255, 0.82);

        font-weight: 500;
      }

      .status-dot {
        width: 7px;
        height: 7px;

        border-radius: 50%;

        background: var(--lime);
      }

      .hero__rail {
        min-height: 74px;

        display: grid;

        grid-template-columns:
          repeat(4, 1fr);

        border-top:
          1px solid
          var(--line);
      }

      .hero__rail-item {
        min-height: 74px;

        display: flex;

        align-items: center;

        justify-content: center;

        gap: 12px;

        border-right:
          1px solid
          var(--line);
      }

      .hero__rail-item:last-child {
        border-right: 0;
      }

      .hero__rail-item span {
        font-family: "DM Mono", monospace;

        font-size: 9px;

        color: var(--ink);
      }

      .hero__rail-item small {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }


      /* =====================================================
         SIGNAL
      ====================================================== */

      .signal {
        padding: 110px 0;

        background: var(--navy);

        color: white;
      }

      .signal__grid {
        display: grid;

        grid-template-columns:
          minmax(260px, 0.7fr)
          minmax(0, 1.3fr);

        gap: 90px;
      }

      .signal__intro h2 {
        max-width: 480px;

        margin: 22px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          46px,
          5vw,
          70px
        );

        line-height: 0.95;

        letter-spacing: -0.065em;

        font-weight: 600;
      }

      .signal__intro h2 span {
        display: block;

        color: var(--lime);
      }

      .signal__cards {
        display: grid;

        grid-template-columns:
          repeat(3, minmax(0, 1fr));

        border-top:
          1px solid
          rgba(255,255,255,0.13);

        border-bottom:
          1px solid
          rgba(255,255,255,0.13);
      }

      .signal-card {
        min-height: 275px;

        padding: 22px;

        display: flex;

        flex-direction: column;

        justify-content: space-between;

        border-right:
          1px solid
          rgba(255,255,255,0.13);
      }

      .signal-card:last-child {
        border-right: 0;
      }

      .signal-card > span {
        color:
          rgba(255,255,255,0.38);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .signal-card strong {
        display: block;

        font-family: "Space Grotesk", sans-serif;

        font-size: 24px;

        font-weight: 600;
      }

      .signal-card p {
        max-width: 220px;

        margin: 10px 0 0;

        color:
          rgba(255,255,255,0.56);

        font-size: 12px;

        line-height: 1.75;
      }


      /* =====================================================
         EXPLORE
      ====================================================== */

      .explore {
        padding: 120px 0;

        background: var(--surface);
      }

      .section-header {
        margin-bottom: 55px;

        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          360px;

        gap: 70px;

        align-items: end;
      }

      .section-header h2 {
        max-width: 700px;

        margin: 19px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.5vw,
          78px
        );

        line-height: 0.93;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .section-header h2 span {
        display: block;

        color: var(--blue);
      }

      .section-header > p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 14px;

        line-height: 1.8;
      }

      .service-grid {
        display: grid;

        grid-template-columns:
          repeat(2, minmax(0, 1fr));

        gap: 12px;
      }

      .service-card {
        position: relative;

        min-height: 350px;

        padding: 24px;

        display: flex;

        flex-direction: column;

        overflow: hidden;

        border:
          1px solid
          var(--line);

        background: var(--bg);

        transition:
          transform 180ms ease,
          border-color 180ms ease,
          background 180ms ease;
      }

      .service-card:hover {
        transform: translateY(-4px);

        border-color: var(--ink);

        background: white;
      }

      .service-card--01 {
        min-height: 430px;
      }

      .service-card--02 {
        transform-origin: center;
      }

      .service-card--02:hover {
        transform: translateY(-4px);
      }

      .service-card--03 {
        min-height: 310px;
      }

      .service-card--04 {
        min-height: 310px;
      }

      .service-card__top {
        display: flex;

        align-items: center;
        justify-content: space-between;
      }

      .service-card__top > span:first-child {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .service-card__type {
        padding: 6px 8px;

        background: white;

        border:
          1px solid
          var(--line);

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .service-card__icon {
        width: 60px;
        height: 60px;

        margin-top: auto;

        margin-bottom: 23px;

        display: grid;

        place-items: center;

        background: var(--ink);

        color: var(--lime);
      }

      .service-card__body h3 {
        max-width: 420px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 30px;

        line-height: 1;

        letter-spacing: -0.045em;

        font-weight: 600;
      }

      .service-card__body p {
        max-width: 450px;

        margin: 13px 0 0;

        color: var(--ink-soft);

        font-size: 12px;

        line-height: 1.75;
      }

      .service-card__bottom {
        margin-top: 31px;

        padding-top: 15px;

        display: flex;

        align-items: center;
        justify-content: space-between;

        border-top:
          1px solid
          var(--line);

        color: var(--ink);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }


      /* =====================================================
         CONTEXT
      ====================================================== */

      .context {
        padding: 120px 0;

        background: var(--bg-soft);

        border-top:
          1px solid
          var(--line);

        border-bottom:
          1px solid
          var(--line);
      }

      .context__grid {
        display: grid;

        grid-template-columns:
          170px
          minmax(0, 1fr);

        gap: 70px;
      }

      .context__side {
        min-height: 520px;

        display: flex;

        flex-direction: column;
      }

      .context__side-number {
        margin-top: auto;

        color: rgba(16, 24, 32, 0.12);

        font-family: "Space Grotesk", sans-serif;

        font-size: 130px;

        line-height: 0.8;

        letter-spacing: -0.1em;
      }

      .context__side-line {
        width: 44px;
        height: 2px;

        margin-top: 20px;

        background: var(--blue);
      }

      .context__side > small {
        margin-top: 16px;

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        line-height: 1.9;

        letter-spacing: 0.09em;
      }

      .context__content {
        max-width: 860px;
      }

      .context__lead {
        margin: 0 0 22px;

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;

        letter-spacing: 0.08em;

        text-transform: uppercase;
      }

      .context__heading h2 {
        max-width: 840px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.7vw,
          79px
        );

        line-height: 0.94;

        letter-spacing: -0.075em;

        font-weight: 600;
      }

      .context__heading h2 span {
        color: var(--blue);
      }

      .context__bottom {
        margin-top: 70px;

        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          250px;

        gap: 70px;

        align-items: end;
      }

      .context__bottom > p {
        max-width: 570px;

        margin: 0;

        color: var(--ink-soft);

        font-size: 14px;

        line-height: 1.85;
      }

      .context__formula {
        padding-top: 17px;

        display: flex;

        flex-wrap: wrap;

        gap: 7px 9px;

        border-top:
          1px solid
          var(--ink);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        line-height: 1.6;

        letter-spacing: 0.06em;
      }

      .context__formula i {
        color: var(--blue);

        font-style: normal;
      }


      /* =====================================================
         METHOD
      ====================================================== */

      .method {
        padding: 125px 0;

        background: var(--surface);
      }

      .method__header {
        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          380px;

        gap: 80px;

        align-items: end;
      }

      .method__header h2 {
        max-width: 680px;

        margin: 20px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.4vw,
          75px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .method__header h2 span {
        display: block;

        color: var(--blue);
      }

      .method__header > p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 14px;

        line-height: 1.8;
      }

      .timeline {
        position: relative;

        margin-top: 85px;

        border-top:
          1px solid
          var(--ink);
      }

      .timeline::before {
        content: "";

        position: absolute;

        left: 0;
        right: 0;

        top: 45px;

        height: 1px;

        background: var(--line);
      }

      .timeline__item {
        position: relative;

        display: grid;

        grid-template-columns:
          90px
          60px
          minmax(0, 1fr);

        gap: 18px;

        min-height: 330px;

        padding-top: 22px;

        border-right:
          1px solid
          var(--line);
      }

      .timeline {
        display: grid;

        grid-template-columns:
          repeat(3, 1fr);
      }

      .timeline__item:last-child {
        border-right: 0;
      }

      .timeline__index {
        padding-left: 2px;

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .timeline__marker {
        position: relative;

        z-index: 2;

        width: 38px;
        height: 38px;

        margin-top: -8px;

        display: grid;

        place-items: center;

        background: var(--surface);

        border:
          1px solid
          var(--ink);

        border-radius: 50%;
      }

      .timeline__marker span {
        width: 9px;
        height: 9px;

        border-radius: 50%;

        background: var(--lime);
      }

      .timeline__copy {
        padding:
          48px
          30px
          30px
          0;
      }

      .timeline__copy small {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .timeline__copy h3 {
        margin: 17px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 34px;

        line-height: 1;

        letter-spacing: -0.05em;

        font-weight: 600;
      }

      .timeline__copy p {
        max-width: 260px;

        margin: 14px 0 0;

        color: var(--ink-soft);

        font-size: 12px;

        line-height: 1.8;
      }


      /* =====================================================
         AREAS
      ====================================================== */

      .areas {
        padding: 120px 0;

        background: var(--bg);
      }

      .areas__header {
        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          350px;

        gap: 80px;

        align-items: end;

        margin-bottom: 55px;
      }

      .areas__header h2 {
        margin: 19px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.5vw,
          76px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .areas__header h2 span {
        color: var(--blue);
      }

      .areas__header p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 14px;

        line-height: 1.8;
      }

      .areas__list {
        display: grid;

        grid-template-columns:
          repeat(3, minmax(0, 1fr));

        gap: 10px;
      }

      .area-card {
        min-height: 325px;

        padding: 22px;

        display: flex;

        flex-direction: column;

        border:
          1px solid
          var(--line);

        background: var(--surface);

        transition:
          transform 170ms ease,
          border-color 170ms ease;
      }

      .area-card:hover {
        transform: translateY(-4px);

        border-color: var(--ink);
      }

      .area-card__number {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .area-card__main {
        margin-top: auto;
      }

      .area-card__main > span {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .area-card__main h3 {
        margin: 11px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 30px;

        line-height: 0.98;

        letter-spacing: -0.05em;

        font-weight: 600;
      }

      .area-card__main p {
        margin: 11px 0 0;

        color: var(--muted);

        font-size: 11px;
      }

      .area-card__status {
        margin-top: 28px;

        padding-top: 14px;

        display: flex;

        align-items: center;
        justify-content: space-between;

        border-top:
          1px solid
          var(--line);
      }

      .area-card__status span {
        color: var(--navy);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }


      /* =====================================================
         MATERIALS
      ====================================================== */

      .materials {
        padding: 120px 0;

        background: var(--bg-soft);

        border-top:
          1px solid
          var(--line);

        border-bottom:
          1px solid
          var(--line);
      }

      .materials__header {
        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          350px;

        gap: 80px;

        align-items: end;

        margin-bottom: 58px;
      }

      .materials__title h2 {
        max-width: 720px;

        margin: 19px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.3vw,
          74px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .materials__title h2 span {
        display: block;

        color: var(--blue);
      }

      .materials__description > p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 14px;

        line-height: 1.8;
      }

      .text-link {
        margin-top: 22px;

        display: inline-flex;

        align-items: center;

        gap: 8px;

        color: var(--ink);

        font-size: 10px;

        font-weight: 700;
      }

      .text-link:hover {
        text-decoration: underline;

        text-underline-offset: 4px;
      }

      .material-list {
        border-top:
          1px solid
          var(--ink);
      }

      .material-item {
        min-height: 105px;

        display: grid;

        grid-template-columns:
          70px
          80px
          minmax(0, 1fr)
          40px;

        gap: 18px;

        align-items: center;

        border-bottom:
          1px solid
          rgba(16,24,32,0.14);

        transition:
          background 160ms ease,
          padding 160ms ease;
      }

      .material-item:hover {
        padding-inline: 14px;

        background: var(--surface);
      }

      .material-item__no {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .material-item__code {
        color: var(--blue);

        font-family: "DM Mono", monospace;

        font-size: 8px;

        letter-spacing: 0.08em;
      }

      .material-item__main h3 {
        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 22px;

        line-height: 1;

        letter-spacing: -0.035em;

        font-weight: 600;
      }

      .material-item__main p {
        margin: 7px 0 0;

        color: var(--muted);

        font-size: 11px;
      }

      .material-item__arrow {
        justify-self: end;
      }


      /* =====================================================
         ABOUT
      ====================================================== */

      .about {
        padding: 125px 0;

        background: var(--surface);
      }

      .about__header {
        display: flex;

        align-items: flex-end;
        justify-content: space-between;

        gap: 50px;

        margin-bottom: 65px;
      }

      .about__header h2 {
        max-width: 680px;

        margin: 19px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.3vw,
          74px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .about__header h2 span {
        display: block;

        color: var(--blue);
      }

      .about__badge {
        min-height: 42px;

        padding: 0 13px;

        display: inline-flex;

        align-items: center;

        gap: 9px;

        border:
          1px solid
          var(--line);

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        white-space: nowrap;
      }

      .about__grid {
        display: grid;

        grid-template-columns:
          0.9fr
          1.1fr;

        gap: 90px;
      }

      .about__statement {
        padding:
          28px
          30px
          38px;

        background: var(--navy);

        color: white;
      }

      .about__number {
        margin: 0;

        color:
          rgba(255,255,255,0.37);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .about__statement blockquote {
        max-width: 450px;

        margin: 120px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          28px,
          3.1vw,
          43px
        );

        line-height: 1.02;

        letter-spacing: -0.045em;

        font-weight: 500;
      }

      .about__principles {
        border-top:
          1px solid
          var(--ink);
      }

      .about__principles article {
        min-height: 132px;

        display: grid;

        grid-template-columns: 45px 1fr;

        gap: 24px;

        padding: 22px 0;

        border-bottom:
          1px solid
          var(--line);
      }

      .about__principles article > span {
        color: var(--blue);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .about__principles h3 {
        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 24px;

        line-height: 1;

        letter-spacing: -0.035em;

        font-weight: 600;
      }

      .about__principles p {
        max-width: 450px;

        margin: 9px 0 0;

        color: var(--muted);

        font-size: 12px;

        line-height: 1.75;
      }


      /* =====================================================
         CTA
      ====================================================== */

      .cta {
        background: var(--lime);
      }

      .cta__inner {
        min-height: 490px;

        padding:
          30px
          0
          48px;

        display: flex;

        flex-direction: column;

        justify-content: space-between;
      }

      .cta__label {
        display: flex;

        align-items: center;
        justify-content: space-between;

        color: rgba(16,24,32,0.5);

        font-family: "DM Mono", monospace;

        font-size: 8px;

        letter-spacing: 0.08em;
      }

      .cta__content {
        display: flex;

        align-items: flex-end;
        justify-content: space-between;

        gap: 50px;
      }

      .cta__content p {
        max-width: 330px;

        margin: 0 0 20px;

        color: rgba(16,24,32,0.62);

        font-family: "DM Mono", monospace;

        font-size: 8px;

        line-height: 1.6;

        text-transform: uppercase;

        letter-spacing: 0.08em;
      }

      .cta__content h2 {
        max-width: 850px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          58px,
          7vw,
          98px
        );

        line-height: 0.88;

        letter-spacing: -0.08em;

        font-weight: 600;
      }

      .cta__content h2 span {
        color: var(--blue);
      }

      .cta__button {
        min-height: 55px;

        padding: 0 19px;

        display: inline-flex;

        align-items: center;
        justify-content: center;

        gap: 10px;

        flex-shrink: 0;

        background: var(--ink);

        color: white;

        font-size: 10px;

        font-weight: 700;

        transition:
          background 160ms ease,
          transform 160ms ease;
      }

      .cta__button:hover {
        background: var(--navy);

        transform: translateY(-2px);
      }


      /* =====================================================
         FOOTER
      ====================================================== */

      .footer {
        background: var(--ink);

        color: white;
      }

      .footer__top {
        padding:
          74px
          0
          70px;

        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          170px
          190px;

        gap: 70px;
      }

      .brand--footer .brand__name {
        color: white;
      }

      .brand--footer .brand__mark span:nth-child(1),
      .brand--footer .brand__mark span:nth-child(4) {
        background: white;
      }

      .footer__identity p {
        max-width: 330px;

        margin: 22px 0 0;

        color:
          rgba(255,255,255,0.43);

        font-size: 12px;

        line-height: 1.75;
      }

      .footer__column {
        display: flex;

        flex-direction: column;

        align-items: flex-start;
      }

      .footer__column > span {
        margin-bottom: 18px;

        color:
          rgba(255,255,255,0.3);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.09em;
      }

      .footer__column a {
        margin-bottom: 11px;

        color:
          rgba(255,255,255,0.66);

        font-size: 11px;

        transition:
          color 160ms ease;
      }

      .footer__column a:hover {
        color: var(--lime);
      }

      .footer__bottom {
        min-height: 62px;

        display: grid;

        grid-template-columns:
          1fr
          1fr
          1fr;

        align-items: center;

        border-top:
          1px solid
          rgba(255,255,255,0.1);

        color:
          rgba(255,255,255,0.28);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.06em;
      }

      .footer__bottom span:nth-child(2) {
        text-align: center;
      }

      .footer__bottom span:last-child {
        text-align: right;
      }


      /* =====================================================
         FLOATING REPORT
      ====================================================== */

      .floating-report {
        position: fixed;

        right: 22px;
        bottom: 20px;

        z-index: 900;

        min-height: 58px;

        padding:
          7px
          10px
          7px
          7px;

        display: inline-flex;

        align-items: center;

        gap: 10px;

        background: var(--lime);

        color: var(--ink);

        box-shadow:
          0 14px 30px
          rgba(0,0,0,0.14);

        transition:
          transform 160ms ease,
          box-shadow 160ms ease;
      }

      .floating-report:hover {
        transform: translateY(-3px);

        box-shadow:
          0 18px 34px
          rgba(0,0,0,0.17);
      }

      .floating-report__icon {
        width: 37px;
        height: 37px;

        display: grid;

        place-items: center;

        background: var(--ink);

        color: var(--lime);
      }

      .floating-report__copy {
        display: grid;

        gap: 4px;
      }

      .floating-report__copy small {
        color:
          rgba(16,24,32,0.52);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.07em;
      }

      .floating-report__copy strong {
        font-size: 10px;

        font-weight: 800;
      }


      /* =====================================================
         1100
      ====================================================== */

      @media (max-width: 1100px) {

        .desktop-nav {
          gap: 18px;
        }

        .hero__grid {
          grid-template-columns:
            minmax(0, 1fr)
            minmax(330px, 0.76fr);

          gap: 45px;
        }

        .hero h1 {
          font-size: clamp(
            55px,
            7vw,
            82px
          );
        }

        .signal__grid {
          gap: 50px;
        }

        .section-header,
        .method__header,
        .areas__header,
        .materials__header {
          grid-template-columns:
            minmax(0, 1fr)
            310px;

          gap: 45px;
        }

        .context__grid {
          grid-template-columns:
            130px
            minmax(0, 1fr);

          gap: 45px;
        }

      }


      /* =====================================================
         860
      ====================================================== */

      @media (max-width: 860px) {

        .page-width {
          width: min(
            calc(100% - 36px),
            var(--page)
          );
        }

        .desktop-nav,
        .header__report {
          display: none;
        }

        .header__region {
          display: none;
        }

        .header__actions {
          margin-left: auto;
        }

        .menu-button {
          display: inline-flex;
        }

        .mobile-nav.mobile-nav--open {
          display: block;
        }

        .hero__grid {
          min-height: auto;

          grid-template-columns: 1fr;

          gap: 0;
        }

        .hero__copy {
          padding:
            72px
            0
            64px;
        }

        .hero__visual {
          margin-bottom: 0;
        }

        .hero__rail {
          grid-template-columns:
            repeat(2, 1fr);
        }

        .hero__rail-item:nth-child(2) {
          border-right: 0;
        }

        .hero__rail-item:nth-child(-n + 2) {
          border-bottom:
            1px solid
            var(--line);
        }

        .signal__grid {
          grid-template-columns: 1fr;

          gap: 48px;
        }

        .signal__cards {
          grid-template-columns: 1fr;
        }

        .signal-card {
          min-height: 180px;

          border-right: 0;

          border-bottom:
            1px solid
            rgba(255,255,255,0.13);
        }

        .signal-card:last-child {
          border-bottom: 0;
        }

        .section-header,
        .method__header,
        .areas__header,
        .materials__header {
          grid-template-columns: 1fr;

          gap: 28px;
        }

        .service-grid {
          grid-template-columns: 1fr;
        }

        .service-card,
        .service-card--01,
        .service-card--03,
        .service-card--04 {
          min-height: 330px;
        }

        .context__grid {
          grid-template-columns: 1fr;

          gap: 35px;
        }

        .context__side {
          min-height: auto;
        }

        .context__side-number {
          margin-top: 25px;

          font-size: 92px;
        }

        .context__bottom {
          grid-template-columns: 1fr;

          gap: 38px;

          margin-top: 52px;
        }

        .timeline {
          grid-template-columns: 1fr;
        }

        .timeline::before {
          display: none;
        }

        .timeline__item {
          min-height: auto;

          padding-bottom: 40px;

          border-right: 0;

          border-bottom:
            1px solid
            var(--line);

          grid-template-columns:
            70px
            60px
            minmax(0, 1fr);
        }

        .timeline__item:last-child {
          border-bottom: 0;
        }

        .timeline__copy {
          padding:
            45px
            20px
            20px
            0;
        }

        .areas__list {
          grid-template-columns: 1fr;
        }

        .area-card {
          min-height: 270px;
        }

        .about__header {
          align-items: flex-start;

          flex-direction: column;
        }

        .about__grid {
          grid-template-columns: 1fr;

          gap: 45px;
        }

        .about__statement {
          min-height: 370px;
        }

        .footer__top {
          grid-template-columns:
            1fr
            1fr;

          gap: 45px;
        }

        .footer__identity {
          grid-column: 1 / -1;
        }

        .cta__content {
          flex-direction: column;

          align-items: flex-start;

          gap: 35px;
        }

      }


      /* =====================================================
         620
      ====================================================== */

      @media (max-width: 620px) {

        .page-width {
          width: calc(100% - 26px);
        }

        .hero__topline {
          font-size: 6px;
        }

        .hero__topline span:last-child {
          display: none;
        }

        .hero__copy {
          padding:
            58px
            0
            52px;
        }

        .hero h1 {
          font-size: clamp(
            46px,
            13vw,
            66px
          );
        }

        .hero__description {
          margin-top: 29px;

          font-size: 13px;
        }

        .hero__buttons {
          flex-direction: column;

          align-items: stretch;
        }

        .button {
          width: 100%;
        }

        .visual__canvas {
          min-height: 350px;
        }

        .map-shape--a {
          width: 190px;
          height: 260px;

          left: 30px;
        }

        .map-shape--b {
          width: 145px;
          height: 180px;

          right: 10px;
        }

        .map-shape--c {
          width: 120px;
          height: 140px;

          left: 100px;
        }

        .visual__center strong {
          font-size: 37px;
        }

        .hero__rail-item {
          min-height: 58px;
        }

        .hero__rail-item small {
          font-size: 6px;
        }

        .signal,
        .explore,
        .context,
        .method,
        .areas,
        .materials,
        .about {
          padding: 82px 0;
        }

        .signal__intro h2,
        .section-header h2,
        .context__heading h2,
        .method__header h2,
        .areas__header h2,
        .materials__title h2,
        .about__header h2 {
          font-size: 42px;
        }

        .signal-card {
          min-height: 160px;
        }

        .service-card__body h3 {
          font-size: 25px;
        }

        .context__heading h2 {
          line-height: 0.98;
        }

        .context__side-number {
          font-size: 72px;
        }

        .timeline__item {
          grid-template-columns:
            46px
            50px
            minmax(0, 1fr);
        }

        .timeline__copy h3 {
          font-size: 29px;
        }

        .material-item {
          min-height: 96px;

          grid-template-columns:
            35px
            50px
            minmax(0, 1fr)
            25px;

          gap: 10px;
        }

        .material-item__main p {
          display: none;
        }

        .material-item__main h3 {
          font-size: 18px;
        }

        .about__statement {
          min-height: 330px;
        }

        .about__statement blockquote {
          margin-top: 100px;

          font-size: 31px;
        }

        .cta__inner {
          min-height: 430px;

          padding:
            24px
            0
            38px;
        }

        .cta__content h2 {
          font-size: 54px;
        }

        .cta__button {
          width: 100%;
        }

        .footer__top {
          grid-template-columns: 1fr 1fr;

          gap: 38px;
        }

        .footer__bottom {
          min-height: auto;

          grid-template-columns: 1fr;

          gap: 9px;

          padding:
            18px
            0
            95px;
        }

        .footer__bottom span:nth-child(2),
        .footer__bottom span:last-child {
          text-align: left;
        }

        .floating-report {
          left: 11px;
          right: 11px;
          bottom: 11px;
        }

        .floating-report__copy {
          flex: 1;
        }

      }


      /* =====================================================
         420
      ====================================================== */

      @media (max-width: 420px) {

        .brand__name {
          font-size: 14px;
        }

        .hero h1 {
          font-size: 43px;
        }

        .hero__eyebrow {
          font-size: 8px;
        }

        .visual__canvas {
          min-height: 285px;
        }

        .visual__center strong {
          font-size: 31px;
        }

        .signal__intro h2,
        .section-header h2,
        .context__heading h2,
        .method__header h2,
        .areas__header h2,
        .materials__title h2,
        .about__header h2 {
          font-size: 37px;
        }

        .service-card {
          min-height: 295px;
        }

        .service-card__body h3 {
          font-size: 23px;
        }

        .area-card {
          min-height: 245px;
        }

        .about__statement blockquote {
          font-size: 28px;
        }

        .cta__content h2 {
          font-size: 47px;
        }

      }
    </style>
  </body>
</html>
