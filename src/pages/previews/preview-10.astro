---
import {
  ArrowRight,
  ArrowUpRight,
  Building2,
  ChartColumn,
  FileText,
  Map,
  Menu,
  Recycle,
  X,
} from "@lucide/astro";

const BASE_URL = (import.meta.env.BASE_URL || "/").replace(/\/$/, "");

const siteHref = (path: string) => {
  if (
    path.startsWith("http://") ||
    path.startsWith("https://") ||
    path.startsWith("mailto:") ||
    path.startsWith("tel:") ||
    path.startsWith("#") ||
    path.startsWith("data:") ||
    path.startsWith("javascript:")
  ) {
    return path;
  }

  if (path === "/") {
    return `${BASE_URL}/`;
  }

  return `${BASE_URL}${path.startsWith("/") ? path : `/${path}`}`;
};

const services = [
  {
    no: "01",
    title: "Peta",
    label: "SPATIAL",
    text: "Lihat persampahan berdasarkan lokasi dan wilayah di Kabupaten Badung.",
    href: "/map",
    icon: Map,
  },
  {
    no: "02",
    title: "Data",
    label: "INSIGHT",
    text: "Temukan informasi persampahan dalam satu ruang yang mudah dipahami.",
    href: "/dashboard",
    icon: ChartColumn,
  },
  {
    no: "03",
    title: "Organisasi",
    label: "NETWORK",
    text: "Kenali pihak dan jaringan yang membentuk ekosistem persampahan.",
    href: "/organizations",
    icon: Building2,
  },
  {
    no: "04",
    title: "Material",
    label: "MATERIAL",
    text: "Pahami kategori material dan karakter dasar dari setiap jenis sampah.",
    href: "/waste",
    icon: Recycle,
  },
];

const areas = [
  {
    no: "01",
    name: "Badung",
    type: "KABUPATEN",
    status: "TERSEDIA",
  },
  {
    no: "02",
    name: "Mengwi",
    type: "KECAMATAN",
    status: "CONTOH",
  },
  {
    no: "03",
    name: "Abiansemal",
    type: "KECAMATAN",
    status: "CONTOH",
  },
];

const materials = [
  {
    no: "01",
    name: "Organik",
    code: "ORG",
    detail: "Sisa makanan, tumbuhan, dan material organik.",
  },
  {
    no: "02",
    name: "Anorganik",
    code: "ANR",
    detail: "Material non-organik dengan berbagai karakter.",
  },
  {
    no: "03",
    name: "Bahan Berbahaya",
    code: "B3",
    detail: "Material yang membutuhkan perhatian khusus.",
  },
  {
    no: "04",
    name: "Elektronik",
    code: "ELK",
    detail: "Perangkat dan komponen elektronik bekas.",
  },
  {
    no: "05",
    name: "Residu",
    code: "RES",
    detail: "Material sisa yang tidak masuk kategori lain.",
  },
];

const principles = [
  {
    no: "01",
    title: "Kontekstual",
    text: "Informasi ditempatkan bersama lokasi dan konteks yang relevan.",
  },
  {
    no: "02",
    title: "Terbuka",
    text: "Informasi publik disusun agar mudah ditemukan dan dipahami.",
  },
  {
    no: "03",
    title: "Bertanggung jawab",
    text: "Penyajian informasi memperhatikan sumber, konteks, dan penggunaannya.",
  },
];

const currentYear = new Date().getFullYear();
---

<!doctype html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />

    <title>BINSIGHT â€” Badung Waste Intelligence</title>

    <meta
      name="description"
      content="Platform informasi persampahan Kabupaten Badung untuk menemukan, memahami, dan melaporkan kondisi persampahan."
    />

    <meta
      name="theme-color"
      content="#f4f5ef"
    />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />

    <link
      href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Manrope:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <div class="site">

      <!-- =====================================================
           HEADER
      ====================================================== -->

      <header class="header">
        <div class="page-width header__inner">

          <a
            href={siteHref("/")}
            class="brand"
            aria-label="BINSIGHT"
          >
            <span
              class="brand__mark"
              aria-hidden="true"
            >
              <i></i>
              <i></i>
              <i></i>
              <i></i>
            </span>

            <span class="brand__text">
              BINSIGHT
            </span>
          </a>

          <nav
            class="desktop-nav"
            aria-label="Navigasi utama"
          >
            <a
              href={siteHref("/")}
              class="is-active"
            >
              Beranda
            </a>

            <a href={siteHref("/map")}>
              Peta
            </a>

            <a href={siteHref("/dashboard")}>
              Data
            </a>

            <a href={siteHref("/organizations")}>
              Organisasi
            </a>

            <a href={siteHref("/waste")}>
              Material
            </a>
          </nav>

          <div class="header__right">

            <span class="header__location">
              BADUNG / BALI
            </span>

            <a
              href={siteHref("/reports/create")}
              class="header__action"
            >
              Laporan
              <ArrowUpRight
                size={15}
                stroke-width={1.8}
              />
            </a>

            <button
              type="button"
              class="menu-button"
              aria-label="Buka menu"
              aria-expanded="false"
              aria-controls="mobile-nav"
            >
              <Menu
                data-menu-open
                size={21}
                stroke-width={1.8}
              />

              <X
                data-menu-close
                size={21}
                stroke-width={1.8}
              />
            </button>

          </div>

        </div>

        <div
          class="mobile-nav"
          id="mobile-nav"
        >
          <div class="page-width mobile-nav__inner">

            <div class="mobile-nav__top">
              <span>MAIN MENU</span>
              <span>01 â€” 06</span>
            </div>

            <a href={siteHref("/")}>
              <small>01</small>
              <span>Beranda</span>
              <ArrowRight size={16} />
            </a>

            <a href={siteHref("/map")}>
              <small>02</small>
              <span>Peta</span>
              <ArrowRight size={16} />
            </a>

            <a href={siteHref("/dashboard")}>
              <small>03</small>
              <span>Data</span>
              <ArrowRight size={16} />
            </a>

            <a href={siteHref("/organizations")}>
              <small>04</small>
              <span>Organisasi</span>
              <ArrowRight size={16} />
            </a>

            <a href={siteHref("/waste")}>
              <small>05</small>
              <span>Material</span>
              <ArrowRight size={16} />
            </a>

            <a href="#tentang">
              <small>06</small>
              <span>Tentang</span>
              <ArrowRight size={16} />
            </a>

          </div>
        </div>
      </header>


      <main>

        <!-- ===================================================
             HERO
        ==================================================== -->

        <section class="hero">

          <div class="page-width hero__top">
            <div>
              <span>BADUNG WASTE INTELLIGENCE</span>
            </div>

            <div>
              <span>PUBLIC INFORMATION PLATFORM</span>
            </div>
          </div>


          <div class="page-width hero__grid">

            <div class="hero__main">

              <div class="hero__index">
                <span>01</span>
                <span>OVERVIEW</span>
              </div>

              <h1>
                Kenali
                <span>persampahan</span>
                Badung.
              </h1>

              <p class="hero__intro">
                BINSIGHT menyatukan informasi tentang lokasi,
                data, organisasi, material, dan kondisi persampahan
                Kabupaten Badung dalam satu ruang publik.
              </p>

              <div class="hero__actions">

                <a
                  href={siteHref("/map")}
                  class="button button--dark"
                >
                  Jelajahi peta
                  <ArrowRight
                    size={17}
                    stroke-width={1.8}
                  />
                </a>

                <a
                  href={siteHref("/dashboard")}
                  class="button button--light"
                >
                  Lihat data
                  <ArrowUpRight
                    size={16}
                    stroke-width={1.8}
                  />
                </a>

              </div>

            </div>


            <div class="hero__interface">

              <div class="interface__bar">
                <span>LIVE CONTEXT</span>

                <div>
                  <i></i>
                  <span>SYSTEM READY</span>
                </div>
              </div>

              <div class="interface__body">

                <div class="interface__axis interface__axis--vertical">
                  <span>N</span>
                  <span>S</span>
                </div>

                <div class="interface__axis interface__axis--horizontal">
                  <span>W</span>
                  <span>E</span>
                </div>

                <div class="interface__grid"></div>

                <div class="interface__shape interface__shape--one"></div>
                <div class="interface__shape interface__shape--two"></div>

                <div class="interface__cross interface__cross--one">
                  <span></span>
                  <i></i>
                </div>

                <div class="interface__cross interface__cross--two">
                  <span></span>
                  <i></i>
                </div>

                <div class="interface__cross interface__cross--three">
                  <span></span>
                  <i></i>
                </div>

                <div class="interface__center">

                  <div class="interface__center-ring">
                    <span></span>
                  </div>

                  <small>REGION</small>

                  <strong>
                    BADUNG
                  </strong>

                  <span>
                    BALI / INDONESIA
                  </span>

                </div>

                <div class="interface__legend">
                  <div>
                    <i></i>
                    <span>AREA</span>
                  </div>

                  <div>
                    <i></i>
                    <span>CONTEXT</span>
                  </div>

                  <div>
                    <i></i>
                    <span>NETWORK</span>
                  </div>
                </div>

              </div>

              <div class="interface__bottom">

                <div>
                  <small>COORDINATES</small>
                  <strong>03Â° 28' S / 115Â° 08' E</strong>
                </div>

                <div>
                  <small>VIEW</small>
                  <strong>REGIONAL</strong>
                </div>

              </div>

            </div>

          </div>


          <div class="page-width hero__signal">

            <div>
              <span>01</span>
              <small>LOCATION</small>
            </div>

            <div>
              <span>02</span>
              <small>DATA</small>
            </div>

            <div>
              <span>03</span>
              <small>NETWORK</small>
            </div>

            <div>
              <span>04</span>
              <small>MATERIAL</small>
            </div>

            <div>
              <span>05</span>
              <small>REPORT</small>
            </div>

          </div>

        </section>


        <!-- ===================================================
             STATEMENT
        ==================================================== -->

        <section class="statement">

          <div class="page-width statement__grid">

            <div class="statement__meta">
              <span>02</span>
              <small>THE CONTEXT</small>
            </div>

            <div class="statement__content">

              <p class="statement__eyebrow">
                SATU ISU, BANYAK KONTEKS.
              </p>

              <h2>
                Persampahan
                <span>perlu dilihat</span>
                secara utuh.
              </h2>

              <div class="statement__lower">

                <p>
                  Informasi tentang sampah tidak hanya berbicara
                  tentang material. Ia juga berkaitan dengan tempat,
                  manusia, organisasi, dan kondisi di sekitarnya.
                </p>

                <p>
                  BINSIGHT menyatukan konteks tersebut agar informasi
                  publik dapat dibaca dengan lebih jelas dan
                  digunakan dengan lebih baik.
                </p>

              </div>

            </div>

          </div>

        </section>


        <!-- ===================================================
             MODULES
        ==================================================== -->

        <section class="modules">

          <div class="page-width">

            <div class="modules__head">

              <div>
                <span class="section-label">
                  03 / EXPLORE
                </span>

                <h2>
                  Pilih ruang
                  <span>yang ingin dilihat.</span>
                </h2>
              </div>

              <p>
                Mulai dari satu kebutuhan, kemudian lanjutkan
                ke konteks yang berhubungan dengannya.
              </p>

            </div>


            <div class="module-grid">

              {
                services.map((service) => {
                  const Icon = service.icon;

                  return (
                    <a
                      href={siteHref(service.href)}
                      class="module-card"
                    >

                      <div class="module-card__top">

                        <span class="module-card__number">
                          {service.no}
                        </span>

                        <span class="module-card__label">
                          {service.label}
                        </span>

                      </div>


                      <div class="module-card__middle">

                        <div class="module-card__icon">
                          <Icon
                            size={25}
                            stroke-width={1.7}
                          />
                        </div>

                        <ArrowUpRight
                          class="module-card__arrow"
                          size={18}
                          stroke-width={1.6}
                        />

                      </div>


                      <div class="module-card__content">

                        <h3>
                          {service.title}
                        </h3>

                        <p>
                          {service.text}
                        </p>

                      </div>


                      <div class="module-card__footer">

                        <span>
                          OPEN MODULE
                        </span>

                        <span>
                          BINSIGHT / {service.no}
                        </span>

                      </div>

                    </a>
                  );
                })
              }

            </div>

          </div>

        </section>


        <!-- ===================================================
             DARK CONTEXT SECTION
        ==================================================== -->

        <section class="dark-section">

          <div class="page-width">

            <div class="dark-section__top">

              <div class="dark-section__meta">
                <span>04</span>
                <small>THE IDEA</small>
              </div>

              <div class="dark-section__label">
                BADUNG / WASTE / INTELLIGENCE
              </div>

            </div>


            <div class="dark-section__headline">

              <p>
                Memahami lingkungan berarti
                memahami hubungan di dalamnya.
              </p>

              <h2>
                MAP
                <span>+</span>
                DATA
                <span>+</span>
                PEOPLE
                <span>+</span>
                MATERIAL
              </h2>

            </div>


            <div class="dark-section__bottom">

              <p>
                Platform tidak berhenti pada satu titik informasi.
                Setiap bagian dirancang untuk membantu pengguna
                berpindah dari satu konteks ke konteks berikutnya.
              </p>

              <a
                href={siteHref("/map")}
                class="dark-section__link"
              >
                Lihat konteks
                <ArrowRight
                  size={16}
                  stroke-width={1.7}
                />
              </a>

            </div>

          </div>

        </section>


        <!-- ===================================================
             METHOD
        ==================================================== -->

        <section class="method">

          <div class="page-width">

            <div class="method__header">

              <div>
                <span class="section-label">
                  05 / PROCESS
                </span>

                <h2>
                  Dari pertanyaan
                  <span>menjadi konteks.</span>
                </h2>
              </div>

              <p>
                Pengalaman BINSIGHT dibangun sebagai alur sederhana
                yang mengikuti kebutuhan pengguna.
              </p>

            </div>


            <div class="method__grid">

              <article class="method-card">

                <div class="method-card__number">
                  01
                </div>

                <div class="method-card__marker">
                  <span></span>
                </div>

                <div class="method-card__body">

                  <small>
                    LANGKAH PERTAMA
                  </small>

                  <h3>
                    Temukan
                  </h3>

                  <p>
                    Cari lokasi, wilayah, data,
                    organisasi, atau material
                    yang ingin diketahui.
                  </p>

                </div>

              </article>


              <article class="method-card">

                <div class="method-card__number">
                  02
                </div>

                <div class="method-card__marker">
                  <span></span>
                </div>

                <div class="method-card__body">

                  <small>
                    LANGKAH KEDUA
                  </small>

                  <h3>
                    Pahami
                  </h3>

                  <p>
                    Baca informasi yang tersedia
                    dan lihat konteks yang
                    berada di baliknya.
                  </p>

                </div>

              </article>


              <article class="method-card">

                <div class="method-card__number">
                  03
                </div>

                <div class="method-card__marker">
                  <span></span>
                </div>

                <div class="method-card__body">

                  <small>
                    LANGKAH KETIGA
                  </small>

                  <h3>
                    Hubungkan
                  </h3>

                  <p>
                    Kaitkan informasi dengan wilayah,
                    material, organisasi, dan
                    pengalaman masyarakat.
                  </p>

                </div>

              </article>

            </div>

          </div>

        </section>


        <!-- ===================================================
             AREAS
        ==================================================== -->

        <section class="coverage">

          <div class="page-width">

            <div class="coverage__head">

              <div>
                <span class="section-label">
                  06 / COVERAGE
                </span>

                <h2>
                  Melihat Badung
                  <span>dari dekat.</span>
                </h2>
              </div>

              <p>
                Informasi dapat dilihat berdasarkan wilayah
                untuk memahami konteks lokal secara lebih dekat.
              </p>

            </div>


            <div class="coverage__body">

              <div class="coverage__map">

                <div class="coverage__map-grid"></div>

                <div class="coverage__outline coverage__outline--one"></div>
                <div class="coverage__outline coverage__outline--two"></div>
                <div class="coverage__outline coverage__outline--three"></div>

                <div class="coverage__label coverage__label--badung">
                  <span></span>
                  BADUNG
                </div>

                <div class="coverage__label coverage__label--mengwi">
                  <span></span>
                  MENGWI
                </div>

                <div class="coverage__label coverage__label--abiansemal">
                  <span></span>
                  ABIANSEMAL
                </div>

                <div class="coverage__center">
                  <small>AREA VIEW</small>
                  <strong>BADUNG</strong>
                  <span>01 / 03</span>
                </div>

              </div>


              <div class="coverage__list">

                {
                  areas.map((area) => (
                    <a
                      href={siteHref("/map")}
                      class="coverage-row"
                    >

                      <div class="coverage-row__number">
                        {area.no}
                      </div>

                      <div class="coverage-row__name">

                        <small>
                          {area.type}
                        </small>

                        <strong>
                          {area.name}
                        </strong>

                      </div>

                      <div class="coverage-row__status">
                        <span></span>
                        {area.status}
                      </div>

                      <ArrowUpRight
                        size={18}
                        stroke-width={1.6}
                      />

                    </a>
                  ))
                }

              </div>

            </div>

          </div>

        </section>


        <!-- ===================================================
             MATERIALS
        ==================================================== -->

        <section class="material">

          <div class="page-width">

            <div class="material__head">

              <div>
                <span class="section-label">
                  07 / MATERIAL
                </span>

                <h2>
                  Kenali
                  <span>materialnya.</span>
                </h2>
              </div>

              <div>
                <p>
                  Setiap material memiliki karakter dan kebutuhan
                  penanganan yang berbeda. Kenali kategorinya
                  sebelum melihat konteks lainnya.
                </p>

                <a
                  href={siteHref("/waste")}
                  class="inline-link"
                >
                  Lihat semua kategori
                  <ArrowRight
                    size={16}
                    stroke-width={1.7}
                  />
                </a>
              </div>

            </div>


            <div class="material__list">

              {
                materials.map((material) => (
                  <a
                    href={siteHref("/waste")}
                    class="material-row"
                  >

                    <div class="material-row__number">
                      {material.no}
                    </div>

                    <div class="material-row__code">
                      {material.code}
                    </div>

                    <div class="material-row__name">
                      <strong>
                        {material.name}
                      </strong>

                      <p>
                        {material.detail}
                      </p>
                    </div>

                    <ArrowUpRight
                      size={19}
                      stroke-width={1.6}
                    />

                  </a>
                ))
              }

            </div>

          </div>

        </section>


        <!-- ===================================================
             ABOUT
        ==================================================== -->

        <section
          class="about"
          id="tentang"
        >

          <div class="page-width">

            <div class="about__header">

              <div>
                <span class="section-label">
                  08 / ABOUT
                </span>

                <h2>
                  Dibangun
                  <span>untuk digunakan.</span>
                </h2>
              </div>

              <div class="about__badge">
                <span></span>
                PUBLIC INFORMATION
              </div>

            </div>


            <div class="about__grid">

              <div class="about__statement">

                <span class="about__statement-number">
                  08
                </span>

                <p>
                  BINSIGHT mengutamakan informasi
                  yang jelas, relevan, dan dapat
                  dipahami oleh publik.
                </p>

              </div>


              <div class="principle-list">

                {
                  principles.map((principle) => (
                    <article class="principle">

                      <span class="principle__number">
                        {principle.no}
                      </span>

                      <div>

                        <h3>
                          {principle.title}
                        </h3>

                        <p>
                          {principle.text}
                        </p>

                      </div>

                      <ArrowUpRight
                        size={16}
                        stroke-width={1.5}
                      />

                    </article>
                  ))
                }

              </div>

            </div>

          </div>

        </section>


        <!-- ===================================================
             FINAL CTA
        ==================================================== -->

        <section class="final">

          <div class="page-width final__inner">

            <div class="final__top">
              <span>
                09 / BINSIGHT
              </span>

              <span>
                BADUNG WASTE INTELLIGENCE
              </span>
            </div>


            <div class="final__main">

              <div>

                <p>
                  MULAI DARI INFORMASI YANG
                  INGIN ANDA KETAHUI.
                </p>

                <h2>
                  Kenali.
                  <span>Pahami.</span>
                  Jaga Badung.
                </h2>

              </div>


              <a
                href={siteHref("/map")}
                class="final__button"
              >
                Jelajahi BINSIGHT
                <ArrowUpRight
                  size={19}
                  stroke-width={1.7}
                />
              </a>

            </div>

          </div>

        </section>

      </main>


      <!-- =====================================================
           FOOTER
      ====================================================== -->

      <footer class="footer">

        <div class="page-width footer__grid">

          <div class="footer__identity">

            <a
              href={siteHref("/")}
              class="brand brand--footer"
            >
              <span
                class="brand__mark"
                aria-hidden="true"
              >
                <i></i>
                <i></i>
                <i></i>
                <i></i>
              </span>

              <span class="brand__text">
                BINSIGHT
              </span>
            </a>

            <p>
              Badung Waste Intelligence.
              Platform informasi publik
              persampahan Kabupaten Badung.
            </p>

          </div>


          <div class="footer__column">

            <span>
              PLATFORM
            </span>

            <a href={siteHref("/map")}>
              Peta
            </a>

            <a href={siteHref("/dashboard")}>
              Data
            </a>

            <a href={siteHref("/organizations")}>
              Organisasi
            </a>

            <a href={siteHref("/waste")}>
              Material
            </a>

          </div>


          <div class="footer__column">

            <span>
              INFORMASI
            </span>

            <a href="#tentang">
              Tentang BINSIGHT
            </a>

            <a href={siteHref("/reports/create")}>
              Laporan masyarakat
            </a>

            <a href={siteHref("/map")}>
              Eksplorasi
            </a>

          </div>

        </div>


        <div class="page-width footer__bottom">

          <span>
            Â© {currentYear} BINSIGHT
          </span>

          <span>
            BADUNG / BALI
          </span>

          <span>
            PUBLIC INFORMATION PLATFORM
          </span>

        </div>

      </footer>


      <!-- =====================================================
           FLOATING REPORT
      ====================================================== -->

      <a
        href={siteHref("/reports/create")}
        class="floating-report"
        aria-label="Buat laporan masyarakat"
      >

        <span class="floating-report__icon">
          <FileText
            size={17}
            stroke-width={1.8}
          />
        </span>

        <span class="floating-report__text">

          <small>
            LAPORAN MASYARAKAT
          </small>

          <strong>
            Buat laporan
          </strong>

        </span>

        <ArrowUpRight
          size={16}
          stroke-width={1.7}
        />

      </a>

    </div>


    <script>
      const menuButton =
        document.querySelector<HTMLButtonElement>(
          ".menu-button"
        );

      const mobileNav =
        document.querySelector<HTMLElement>(
          ".mobile-nav"
        );

      const menuOpen =
        document.querySelector<SVGElement>(
          "[data-menu-open]"
        );

      const menuClose =
        document.querySelector<SVGElement>(
          "[data-menu-close]"
        );

      const updateMenu = (open: boolean) => {
        if (!menuButton || !mobileNav) {
          return;
        }

        menuButton.setAttribute(
          "aria-expanded",
          String(open)
        );

        menuButton.setAttribute(
          "aria-label",
          open ? "Tutup menu" : "Buka menu"
        );

        mobileNav.classList.toggle(
          "mobile-nav--open",
          open
        );

        if (menuOpen) {
          menuOpen.style.display =
            open ? "none" : "block";
        }

        if (menuClose) {
          menuClose.style.display =
            open ? "block" : "none";
        }
      };

      updateMenu(false);

      menuButton?.addEventListener(
        "click",
        () => {
          const isOpen =
            menuButton.getAttribute(
              "aria-expanded"
            ) === "true";

          updateMenu(!isOpen);
        }
      );

      document
        .querySelectorAll<HTMLAnchorElement>(
          ".mobile-nav a"
        )
        .forEach((link) => {
          link.addEventListener(
            "click",
            () => updateMenu(false)
          );
        });

      window.addEventListener(
        "resize",
        () => {
          if (window.innerWidth > 860) {
            updateMenu(false);
          }
        }
      );
    </script>


    <style>
      :root {
        --bg: #f4f5ef;
        --bg-soft: #e9ebe3;
        --surface: #ffffff;

        --ink: #131719;
        --ink-soft: #4f585d;
        --muted: #7d8588;

        --line: #d4d8d3;

        --dark: #17282f;
        --dark-soft: #21404b;

        --lime: #d6f45f;
        --blue: #466cff;

        --page: 1260px;
      }


      /* =====================================================
         RESET
      ====================================================== */

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        margin: 0;

        background: var(--bg);

        color: var(--ink);

        font-family: "Manrope", sans-serif;

        -webkit-font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
      }

      a {
        color: inherit;
        text-decoration: none;
      }

      button {
        font: inherit;
      }

      svg {
        flex-shrink: 0;
      }

      .site {
        min-height: 100vh;

        overflow: clip;
      }

      .page-width {
        width:
          min(
            calc(100% - 64px),
            var(--page)
          );

        margin-inline: auto;
      }


      /* =====================================================
         HEADER
      ====================================================== */

      .header {
        position: sticky;

        top: 0;

        z-index: 500;

        background:
          rgba(244, 245, 239, 0.9);

        backdrop-filter: blur(18px);

        border-bottom:
          1px solid
          var(--line);
      }

      .header__inner {
        min-height: 74px;

        display: flex;

        align-items: center;

        gap: 26px;
      }

      .brand {
        display: inline-flex;

        align-items: center;

        gap: 11px;

        flex-shrink: 0;
      }

      .brand__mark {
        width: 27px;
        height: 27px;

        display: grid;

        grid-template-columns:
          repeat(2, 1fr);

        grid-template-rows:
          repeat(2, 1fr);

        gap: 2px;
      }

      .brand__mark i {
        display: block;

        background: var(--ink);
      }

      .brand__mark i:nth-child(2) {
        background: var(--lime);
      }

      .brand__mark i:nth-child(3) {
        background: var(--blue);
      }

      .brand__mark i:nth-child(4) {
        background: var(--ink);
      }

      .brand__text {
        font-size: 15px;

        line-height: 1;

        font-weight: 800;

        letter-spacing: -0.03em;
      }

      .desktop-nav {
        display: flex;

        align-items: center;

        gap: 28px;

        margin-inline: auto;
      }

      .desktop-nav a {
        position: relative;

        color:
          #687075;

        font-size: 11px;

        font-weight: 600;

        transition:
          color 160ms ease;
      }

      .desktop-nav a:hover {
        color: var(--ink);
      }

      .desktop-nav a.is-active {
        color: var(--ink);
      }

      .desktop-nav a.is-active::after {
        content: "";

        position: absolute;

        left: 0;
        right: 0;

        bottom: -12px;

        height: 2px;

        background: var(--blue);
      }

      .header__right {
        display: flex;

        align-items: center;

        gap: 15px;
      }

      .header__location {
        color: #8a9194;

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .header__action {
        min-height: 38px;

        padding-inline: 13px;

        display: inline-flex;

        align-items: center;
        justify-content: center;

        gap: 8px;

        background: var(--ink);

        color: white;

        font-size: 10px;

        font-weight: 700;

        transition:
          background 160ms ease;
      }

      .header__action:hover {
        background: var(--dark);
      }

      .menu-button {
        width: 40px;
        height: 40px;

        display: none;

        align-items: center;
        justify-content: center;

        border:
          1px solid
          var(--line);

        background: transparent;

        color: var(--ink);

        cursor: pointer;

        padding: 0;
      }

      .menu-button [data-menu-close] {
        display: none;
      }

      .mobile-nav {
        display: none;

        border-top:
          1px solid
          var(--line);
      }

      .mobile-nav--open {
        display: block;
      }

      .mobile-nav__inner {
        padding: 16px 0 20px;
      }

      .mobile-nav__top {
        min-height: 34px;

        display: flex;

        align-items: center;
        justify-content: space-between;

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .mobile-nav a {
        min-height: 57px;

        display: grid;

        grid-template-columns:
          34px
          minmax(0, 1fr)
          20px;

        align-items: center;

        gap: 10px;

        border-top:
          1px solid
          var(--line);

        font-size: 14px;

        font-weight: 700;
      }

      .mobile-nav a small {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .mobile-nav a svg {
        justify-self: end;

        color: var(--blue);
      }


      /* =====================================================
         HERO
      ====================================================== */

      .hero {
        background: var(--bg);
      }

      .hero__top {
        min-height: 44px;

        display: flex;

        align-items: center;
        justify-content: space-between;

        border-bottom:
          1px solid
          var(--line);

        color: #858d90;

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .hero__grid {
        min-height: 650px;

        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          minmax(420px, 0.84fr);

        gap: 75px;

        align-items: center;
      }

      .hero__main {
        padding: 80px 0;
      }

      .hero__index {
        display: flex;

        align-items: center;

        gap: 12px;

        margin-bottom: 28px;
      }

      .hero__index span:first-child {
        width: 28px;
        height: 28px;

        display: grid;

        place-items: center;

        border:
          1px solid
          var(--ink);

        border-radius: 50%;

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .hero__index span:last-child {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.09em;
      }

      .hero h1 {
        max-width: 760px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          64px,
          8vw,
          108px
        );

        line-height: 0.88;

        letter-spacing: -0.075em;

        font-weight: 600;
      }

      .hero h1 span {
        display: block;

        color: var(--dark);
      }

      .hero__intro {
        max-width: 520px;

        margin: 34px 0 0;

        color: var(--ink-soft);

        font-size: 14px;

        line-height: 1.85;
      }

      .hero__actions {
        margin-top: 31px;

        display: flex;

        gap: 10px;

        flex-wrap: wrap;
      }

      .button {
        min-height: 50px;

        padding-inline: 18px;

        display: inline-flex;

        align-items: center;
        justify-content: center;

        gap: 10px;

        font-size: 10px;

        font-weight: 800;

        transition:
          transform 160ms ease,
          background 160ms ease,
          border-color 160ms ease;
      }

      .button:hover {
        transform: translateY(-2px);
      }

      .button--dark {
        background: var(--ink);

        color: white;
      }

      .button--dark:hover {
        background: var(--dark);
      }

      .button--light {
        border:
          1px solid
          var(--line);

        background: transparent;

        color: var(--ink);
      }

      .button--light:hover {
        border-color: var(--ink);
      }

      .hero__interface {
        position: relative;

        background: var(--dark);

        color: white;

        box-shadow:
          18px 18px 0
          rgba(19, 23, 25, 0.06);
      }

      .interface__bar {
        min-height: 49px;

        padding-inline: 18px;

        display: flex;

        align-items: center;
        justify-content: space-between;

        border-bottom:
          1px solid
          rgba(255,255,255,0.12);

        color:
          rgba(255,255,255,0.43);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .interface__bar > div {
        display: flex;

        align-items: center;

        gap: 7px;
      }

      .interface__bar i {
        width: 6px;
        height: 6px;

        border-radius: 50%;

        background: var(--lime);
      }

      .interface__body {
        position: relative;

        min-height: 445px;

        overflow: hidden;

        background:
          #1a313b;
      }

      .interface__grid {
        position: absolute;

        inset: 0;

        opacity: 0.32;

        background-image:
          linear-gradient(
            rgba(255,255,255,0.05) 1px,
            transparent 1px
          ),
          linear-gradient(
            90deg,
            rgba(255,255,255,0.05) 1px,
            transparent 1px
          );

        background-size: 40px 40px;
      }

      .interface__shape {
        position: absolute;

        border:
          1px solid
          rgba(214,244,95,0.2);

        background:
          rgba(214,244,95,0.025);

        transform: rotate(-14deg);
      }

      .interface__shape--one {
        width: 280px;
        height: 350px;

        left: 78px;
        top: 53px;

        border-radius:
          52% 48% 58% 42%;
      }

      .interface__shape--two {
        width: 180px;
        height: 220px;

        right: 35px;
        top: 105px;

        border-radius:
          40% 60% 35% 65%;

        transform:
          rotate(18deg);
      }

      .interface__axis {
        position: absolute;

        z-index: 3;

        display: flex;

        justify-content: space-between;

        color:
          rgba(255,255,255,0.3);

        font-family: "DM Mono", monospace;

        font-size: 6px;
      }

      .interface__axis--vertical {
        top: 18px;
        bottom: 18px;

        left: 16px;

        flex-direction: column;
      }

      .interface__axis--horizontal {
        left: 55px;
        right: 55px;

        bottom: 14px;
      }

      .interface__cross {
        position: absolute;

        z-index: 5;

        width: 24px;
        height: 24px;
      }

      .interface__cross span,
      .interface__cross i {
        position: absolute;

        background: var(--lime);
      }

      .interface__cross span {
        left: 50%;
        top: 0;
        bottom: 0;

        width: 1px;

        transform:
          translateX(-50%);
      }

      .interface__cross i {
        top: 50%;
        left: 0;
        right: 0;

        height: 1px;

        transform:
          translateY(-50%);
      }

      .interface__cross::after {
        content: "";

        position: absolute;

        left: 50%;
        top: 50%;

        width: 6px;
        height: 6px;

        transform:
          translate(-50%, -50%);

        border:
          1px solid
          var(--lime);

        border-radius: 50%;

        background: var(--dark);
      }

      .interface__cross--one {
        left: 28%;
        top: 25%;
      }

      .interface__cross--two {
        right: 22%;
        top: 47%;
      }

      .interface__cross--three {
        left: 43%;
        bottom: 18%;
      }

      .interface__center {
        position: absolute;

        z-index: 4;

        left: 50%;
        top: 50%;

        transform:
          translate(-50%, -50%);

        display: flex;

        align-items: center;

        flex-direction: column;

        text-align: center;
      }

      .interface__center-ring {
        width: 62px;
        height: 62px;

        margin-bottom: 12px;

        display: grid;

        place-items: center;

        border:
          1px solid
          rgba(214,244,95,0.35);

        border-radius: 50%;
      }

      .interface__center-ring span {
        width: 13px;
        height: 13px;

        border-radius: 50%;

        background: var(--lime);

        box-shadow:
          0 0 0 7px
          rgba(214,244,95,0.08);
      }

      .interface__center small {
        color:
          rgba(255,255,255,0.4);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.12em;
      }

      .interface__center strong {
        margin-top: 7px;

        font-family: "Space Grotesk", sans-serif;

        font-size: 37px;

        line-height: 0.95;

        letter-spacing: -0.06em;
      }

      .interface__center > span {
        margin-top: 8px;

        color: var(--lime);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .interface__legend {
        position: absolute;

        z-index: 5;

        left: 20px;
        bottom: 18px;

        display: flex;

        gap: 16px;
      }

      .interface__legend div {
        display: flex;

        align-items: center;

        gap: 6px;

        color:
          rgba(255,255,255,0.43);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.06em;
      }

      .interface__legend i {
        width: 5px;
        height: 5px;

        display: block;

        border-radius: 50%;

        background: var(--lime);
      }

      .interface__legend div:nth-child(2) i {
        background: white;
      }

      .interface__legend div:nth-child(3) i {
        background: var(--blue);
      }

      .interface__bottom {
        min-height: 57px;

        padding: 0 18px;

        display: grid;

        grid-template-columns: 1fr 1fr;

        align-items: center;

        border-top:
          1px solid
          rgba(255,255,255,0.12);
      }

      .interface__bottom div {
        display: grid;

        gap: 4px;
      }

      .interface__bottom div:last-child {
        justify-items: end;
      }

      .interface__bottom small {
        color:
          rgba(255,255,255,0.3);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;
      }

      .interface__bottom strong {
        color:
          rgba(255,255,255,0.75);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        font-weight: 500;

        letter-spacing: 0.05em;
      }

      .hero__signal {
        min-height: 72px;

        display: grid;

        grid-template-columns:
          repeat(5, 1fr);

        border-top:
          1px solid
          var(--line);
      }

      .hero__signal > div {
        display: flex;

        align-items: center;
        justify-content: center;

        gap: 10px;

        border-right:
          1px solid
          var(--line);
      }

      .hero__signal > div:last-child {
        border-right: 0;
      }

      .hero__signal span {
        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .hero__signal small {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;
      }


      /* =====================================================
         STATEMENT
      ====================================================== */

      .statement {
        padding: 118px 0;

        background: var(--surface);

        border-bottom:
          1px solid
          var(--line);
      }

      .statement__grid {
        display: grid;

        grid-template-columns:
          170px
          minmax(0, 1fr);

        gap: 78px;
      }

      .statement__meta {
        display: flex;

        flex-direction: column;

        gap: 7px;
      }

      .statement__meta span {
        color: var(--blue);

        font-family: "DM Mono", monospace;

        font-size: 9px;
      }

      .statement__meta small {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.09em;
      }

      .statement__content {
        max-width: 880px;
      }

      .statement__eyebrow {
        margin: 0 0 24px;

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;

        letter-spacing: 0.1em;
      }

      .statement h2 {
        max-width: 900px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          53px,
          6.2vw,
          84px
        );

        line-height: 0.93;

        letter-spacing: -0.075em;

        font-weight: 600;
      }

      .statement h2 span {
        display: block;

        color: var(--dark);
      }

      .statement__lower {
        max-width: 730px;

        margin-top: 42px;

        display: grid;

        grid-template-columns:
          1fr
          1fr;

        gap: 40px;
      }

      .statement__lower p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 13px;

        line-height: 1.8;
      }


      /* =====================================================
         MODULES
      ====================================================== */

      .modules {
        padding: 120px 0;

        background: var(--bg);
      }

      .modules__head {
        margin-bottom: 58px;

        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          350px;

        gap: 80px;

        align-items: end;
      }

      .section-label {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.1em;
      }

      .modules__head h2 {
        max-width: 700px;

        margin: 18px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          49px,
          5.5vw,
          78px
        );

        line-height: 0.94;

        letter-spacing: -0.075em;

        font-weight: 600;
      }

      .modules__head h2 span {
        display: block;

        color: var(--blue);
      }

      .modules__head > p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 13px;

        line-height: 1.8;
      }

      .module-grid {
        display: grid;

        grid-template-columns:
          repeat(2, minmax(0, 1fr));

        gap: 12px;
      }

      .module-card {
        min-height: 350px;

        padding: 23px;

        display: flex;

        flex-direction: column;

        border:
          1px solid
          var(--line);

        background: var(--surface);

        transition:
          transform 170ms ease,
          border-color 170ms ease,
          box-shadow 170ms ease;
      }

      .module-card:hover {
        transform: translateY(-4px);

        border-color: var(--ink);

        box-shadow:
          12px 12px 0
          rgba(19, 23, 25, 0.045);
      }

      .module-card__top,
      .module-card__footer {
        display: flex;

        align-items: center;
        justify-content: space-between;
      }

      .module-card__number {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .module-card__label {
        padding: 6px 8px;

        border:
          1px solid
          var(--line);

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;
      }

      .module-card__middle {
        margin-top: auto;

        display: flex;

        align-items: center;
        justify-content: space-between;
      }

      .module-card__icon {
        width: 58px;
        height: 58px;

        display: grid;

        place-items: center;

        background: var(--dark);

        color: var(--lime);
      }

      .module-card__arrow {
        color: var(--blue);
      }

      .module-card__content {
        margin-top: 22px;
      }

      .module-card__content h3 {
        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 29px;

        line-height: 1;

        letter-spacing: -0.05em;

        font-weight: 600;
      }

      .module-card__content p {
        max-width: 470px;

        margin: 11px 0 0;

        color: var(--ink-soft);

        font-size: 12px;

        line-height: 1.75;
      }

      .module-card__footer {
        margin-top: 29px;

        padding-top: 14px;

        border-top:
          1px solid
          var(--line);

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;
      }


      /* =====================================================
         DARK SECTION
      ====================================================== */

      .dark-section {
        padding: 105px 0;

        background: var(--dark);

        color: white;
      }

      .dark-section__top {
        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 20px;

        padding-bottom: 26px;

        border-bottom:
          1px solid
          rgba(255,255,255,0.12);
      }

      .dark-section__meta {
        display: flex;

        align-items: center;

        gap: 11px;
      }

      .dark-section__meta span {
        width: 26px;
        height: 26px;

        display: grid;

        place-items: center;

        border:
          1px solid
          rgba(255,255,255,0.35);

        border-radius: 50%;

        color: var(--lime);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .dark-section__meta small,
      .dark-section__label {
        color:
          rgba(255,255,255,0.38);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.09em;
      }

      .dark-section__headline {
        padding:
          76px
          0
          66px;
      }

      .dark-section__headline p {
        max-width: 420px;

        margin: 0 0 30px;

        color:
          rgba(255,255,255,0.5);

        font-family: "DM Mono", monospace;

        font-size: 8px;

        line-height: 1.7;

        letter-spacing: 0.08em;

        text-transform: uppercase;
      }

      .dark-section__headline h2 {
        max-width: 1050px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          6.8vw,
          95px
        );

        line-height: 0.88;

        letter-spacing: -0.08em;

        font-weight: 600;
      }

      .dark-section__headline h2 span {
        color: var(--lime);
      }

      .dark-section__bottom {
        display: flex;

        align-items: flex-end;
        justify-content: space-between;

        gap: 40px;

        padding-top: 23px;

        border-top:
          1px solid
          rgba(255,255,255,0.12);
      }

      .dark-section__bottom p {
        max-width: 510px;

        margin: 0;

        color:
          rgba(255,255,255,0.53);

        font-size: 13px;

        line-height: 1.8;
      }

      .dark-section__link {
        min-height: 46px;

        padding: 0 14px;

        display: inline-flex;

        align-items: center;

        gap: 9px;

        background: white;

        color: var(--ink);

        font-size: 10px;

        font-weight: 700;
      }


      /* =====================================================
         METHOD
      ====================================================== */

      .method {
        padding: 120px 0;

        background: var(--surface);
      }

      .method__header {
        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          340px;

        gap: 80px;

        align-items: end;
      }

      .method__header h2 {
        max-width: 690px;

        margin: 18px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.4vw,
          76px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .method__header h2 span {
        display: block;

        color: var(--blue);
      }

      .method__header > p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 13px;

        line-height: 1.8;
      }

      .method__grid {
        margin-top: 78px;

        display: grid;

        grid-template-columns:
          repeat(3, minmax(0, 1fr));

        border-top:
          1px solid
          var(--ink);
      }

      .method-card {
        min-height: 305px;

        display: grid;

        grid-template-columns:
          48px
          44px
          minmax(0, 1fr);

        gap: 15px;

        padding-top: 22px;

        border-right:
          1px solid
          var(--line);
      }

      .method-card:last-child {
        border-right: 0;
      }

      .method-card__number {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .method-card__marker {
        position: relative;

        width: 35px;
        height: 35px;

        display: grid;

        place-items: center;

        border:
          1px solid
          var(--ink);

        border-radius: 50%;
      }

      .method-card__marker span {
        width: 8px;
        height: 8px;

        border-radius: 50%;

        background: var(--lime);
      }

      .method-card__body {
        padding:
          42px
          30px
          30px
          0;
      }

      .method-card__body small {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;
      }

      .method-card__body h3 {
        margin: 15px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 32px;

        line-height: 1;

        letter-spacing: -0.05em;

        font-weight: 600;
      }

      .method-card__body p {
        max-width: 270px;

        margin: 13px 0 0;

        color: var(--ink-soft);

        font-size: 12px;

        line-height: 1.8;
      }


      /* =====================================================
         COVERAGE
      ====================================================== */

      .coverage {
        padding: 120px 0;

        background: var(--bg);
      }

      .coverage__head {
        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          340px;

        gap: 80px;

        align-items: end;

        margin-bottom: 55px;
      }

      .coverage__head h2 {
        max-width: 680px;

        margin: 18px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          48px,
          5.5vw,
          77px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .coverage__head h2 span {
        color: var(--blue);
      }

      .coverage__head > p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 13px;

        line-height: 1.8;
      }

      .coverage__body {
        display: grid;

        grid-template-columns:
          1.05fr
          0.95fr;

        gap: 12px;
      }

      .coverage__map {
        position: relative;

        min-height: 470px;

        overflow: hidden;

        background: var(--dark);

        color: white;
      }

      .coverage__map-grid {
        position: absolute;

        inset: 0;

        opacity: 0.25;

        background-image:
          linear-gradient(
            rgba(255,255,255,0.06) 1px,
            transparent 1px
          ),
          linear-gradient(
            90deg,
            rgba(255,255,255,0.06) 1px,
            transparent 1px
          );

        background-size: 35px 35px;
      }

      .coverage__outline {
        position: absolute;

        border:
          1px solid
          rgba(214,244,95,0.28);

        background:
          rgba(214,244,95,0.03);

        transform: rotate(-17deg);
      }

      .coverage__outline--one {
        width: 290px;
        height: 330px;

        left: 90px;
        top: 66px;

        border-radius:
          56% 44% 58% 42%;
      }

      .coverage__outline--two {
        width: 190px;
        height: 220px;

        right: 85px;
        top: 105px;

        border-radius:
          40% 60% 45% 55%;

        transform:
          rotate(18deg);
      }

      .coverage__outline--three {
        width: 150px;
        height: 170px;

        left: 245px;
        bottom: 36px;

        border-radius:
          60% 40% 53% 47%;

        transform:
          rotate(31deg);
      }

      .coverage__label {
        position: absolute;

        z-index: 3;

        display: flex;

        align-items: center;

        gap: 8px;

        color:
          rgba(255,255,255,0.6);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;
      }

      .coverage__label span {
        width: 7px;
        height: 7px;

        border-radius: 50%;

        background: var(--lime);
      }

      .coverage__label--badung {
        top: 26%;
        left: 31%;
      }

      .coverage__label--mengwi {
        right: 23%;
        top: 48%;
      }

      .coverage__label--abiansemal {
        left: 46%;
        bottom: 19%;
      }

      .coverage__center {
        position: absolute;

        left: 50%;
        top: 50%;

        transform:
          translate(-50%, -50%);

        display: flex;

        align-items: center;

        flex-direction: column;

        text-align: center;
      }

      .coverage__center small {
        color:
          rgba(255,255,255,0.38);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.1em;
      }

      .coverage__center strong {
        margin-top: 8px;

        font-family: "Space Grotesk", sans-serif;

        font-size: 38px;

        line-height: 0.95;

        letter-spacing: -0.06em;
      }

      .coverage__center span {
        margin-top: 8px;

        color: var(--lime);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.07em;
      }

      .coverage__list {
        border-top:
          1px solid
          var(--ink);

        border-bottom:
          1px solid
          var(--line);

        background: var(--surface);
      }

      .coverage-row {
        min-height: 156px;

        padding-inline: 20px;

        display: grid;

        grid-template-columns:
          48px
          minmax(0, 1fr)
          100px
          22px;

        gap: 16px;

        align-items: center;

        border-bottom:
          1px solid
          var(--line);

        transition:
          background 160ms ease;
      }

      .coverage-row:last-child {
        border-bottom: 0;
      }

      .coverage-row:hover {
        background: var(--bg);
      }

      .coverage-row__number {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .coverage-row__name small {
        display: block;

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;
      }

      .coverage-row__name strong {
        display: block;

        margin-top: 10px;

        font-family: "Space Grotesk", sans-serif;

        font-size: 23px;

        line-height: 1;

        letter-spacing: -0.04em;

        font-weight: 600;
      }

      .coverage-row__status {
        display: flex;

        align-items: center;

        gap: 7px;

        color: var(--dark);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.07em;
      }

      .coverage-row__status span {
        width: 6px;
        height: 6px;

        display: block;

        border-radius: 50%;

        background: var(--lime);

        border:
          1px solid
          rgba(0,0,0,0.12);
      }

      .coverage-row svg {
        justify-self: end;

        color: var(--blue);
      }


      /* =====================================================
         MATERIAL
      ====================================================== */

      .material {
        padding: 120px 0;

        background: var(--bg-soft);

        border-top:
          1px solid
          var(--line);
      }

      .material__head {
        margin-bottom: 57px;

        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          350px;

        gap: 80px;

        align-items: end;
      }

      .material__head h2 {
        max-width: 680px;

        margin: 18px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          49px,
          5.5vw,
          76px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .material__head h2 span {
        display: block;

        color: var(--blue);
      }

      .material__head > div:last-child > p {
        margin: 0;

        color: var(--ink-soft);

        font-size: 13px;

        line-height: 1.8;
      }

      .inline-link {
        margin-top: 22px;

        display: inline-flex;

        align-items: center;

        gap: 8px;

        font-size: 10px;

        font-weight: 700;
      }

      .inline-link:hover {
        text-decoration: underline;

        text-underline-offset: 4px;
      }

      .material__list {
        border-top:
          1px solid
          var(--ink);
      }

      .material-row {
        min-height: 103px;

        display: grid;

        grid-template-columns:
          55px
          70px
          minmax(0, 1fr)
          22px;

        gap: 18px;

        align-items: center;

        border-bottom:
          1px solid
          rgba(19,23,25,0.15);

        transition:
          background 160ms ease,
          padding 160ms ease;
      }

      .material-row:hover {
        padding-inline: 12px;

        background: var(--surface);
      }

      .material-row__number {
        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .material-row__code {
        color: var(--blue);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .material-row__name strong {
        font-family: "Space Grotesk", sans-serif;

        font-size: 21px;

        line-height: 1;

        letter-spacing: -0.035em;

        font-weight: 600;
      }

      .material-row__name p {
        margin: 7px 0 0;

        color: var(--muted);

        font-size: 10px;
      }

      .material-row svg {
        justify-self: end;

        color: var(--blue);
      }


      /* =====================================================
         ABOUT
      ====================================================== */

      .about {
        padding: 120px 0;

        background: var(--surface);
      }

      .about__header {
        margin-bottom: 58px;

        display: flex;

        align-items: flex-end;
        justify-content: space-between;

        gap: 40px;
      }

      .about__header h2 {
        max-width: 690px;

        margin: 18px 0 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          49px,
          5.5vw,
          76px
        );

        line-height: 0.94;

        letter-spacing: -0.07em;

        font-weight: 600;
      }

      .about__header h2 span {
        display: block;

        color: var(--blue);
      }

      .about__badge {
        min-height: 40px;

        padding-inline: 12px;

        display: inline-flex;

        align-items: center;

        gap: 8px;

        border:
          1px solid
          var(--line);

        color: var(--muted);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.08em;

        white-space: nowrap;
      }

      .about__badge span {
        width: 6px;
        height: 6px;

        display: block;

        border-radius: 50%;

        background: var(--lime);
      }

      .about__grid {
        display: grid;

        grid-template-columns:
          0.8fr
          1.2fr;

        gap: 12px;
      }

      .about__statement {
        min-height: 420px;

        padding: 24px;

        display: flex;

        flex-direction: column;

        justify-content: space-between;

        background: var(--dark);

        color: white;
      }

      .about__statement-number {
        color:
          rgba(255,255,255,0.34);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .about__statement p {
        max-width: 400px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          27px,
          3vw,
          42px
        );

        line-height: 1.02;

        letter-spacing: -0.04em;

        font-weight: 500;
      }

      .principle-list {
        border-top:
          1px solid
          var(--ink);

        background: var(--surface);
      }

      .principle {
        min-height: 140px;

        padding: 20px;

        display: grid;

        grid-template-columns:
          45px
          minmax(0, 1fr)
          20px;

        gap: 18px;

        align-items: start;

        border-bottom:
          1px solid
          var(--line);
      }

      .principle:last-child {
        border-bottom: 0;
      }

      .principle__number {
        color: var(--blue);

        font-family: "DM Mono", monospace;

        font-size: 8px;
      }

      .principle h3 {
        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: 23px;

        line-height: 1;

        letter-spacing: -0.035em;

        font-weight: 600;
      }

      .principle p {
        max-width: 470px;

        margin: 9px 0 0;

        color: var(--muted);

        font-size: 11px;

        line-height: 1.75;
      }

      .principle > svg {
        justify-self: end;

        color: var(--blue);
      }


      /* =====================================================
         FINAL
      ====================================================== */

      .final {
        background: var(--lime);
      }

      .final__inner {
        min-height: 465px;

        padding:
          30px
          0
          45px;

        display: flex;

        flex-direction: column;

        justify-content: space-between;
      }

      .final__top {
        display: flex;

        align-items: center;
        justify-content: space-between;

        color:
          rgba(19,23,25,0.48);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.09em;
      }

      .final__main {
        display: flex;

        align-items: flex-end;
        justify-content: space-between;

        gap: 50px;
      }

      .final__main p {
        margin: 0 0 18px;

        color:
          rgba(19,23,25,0.52);

        font-family: "DM Mono", monospace;

        font-size: 7px;

        letter-spacing: 0.08em;
      }

      .final__main h2 {
        max-width: 900px;

        margin: 0;

        font-family: "Space Grotesk", sans-serif;

        font-size: clamp(
          58px,
          7vw,
          98px
        );

        line-height: 0.87;

        letter-spacing: -0.08em;

        font-weight: 600;
      }

      .final__main h2 span {
        color: var(--blue);
      }

      .final__button {
        min-height: 54px;

        padding-inline: 18px;

        display: inline-flex;

        align-items: center;
        justify-content: center;

        gap: 10px;

        flex-shrink: 0;

        background: var(--ink);

        color: white;

        font-size: 10px;

        font-weight: 700;

        transition:
          background 160ms ease,
          transform 160ms ease;
      }

      .final__button:hover {
        background: var(--dark);

        transform: translateY(-2px);
      }


      /* =====================================================
         FOOTER
      ====================================================== */

      .footer {
        background: var(--ink);

        color: white;
      }

      .footer__grid {
        padding:
          72px
          0
          68px;

        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          170px
          190px;

        gap: 70px;
      }

      .brand--footer .brand__text {
        color: white;
      }

      .brand--footer .brand__mark i:nth-child(1),
      .brand--footer .brand__mark i:nth-child(4) {
        background: white;
      }

      .footer__identity p {
        max-width: 330px;

        margin: 21px 0 0;

        color:
          rgba(255,255,255,0.42);

        font-size: 11px;

        line-height: 1.8;
      }

      .footer__column {
        display: flex;

        flex-direction: column;

        align-items: flex-start;
      }

      .footer__column > span {
        margin-bottom: 17px;

        color:
          rgba(255,255,255,0.27);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.09em;
      }

      .footer__column a {
        margin-bottom: 11px;

        color:
          rgba(255,255,255,0.63);

        font-size: 10px;

        transition:
          color 160ms ease;
      }

      .footer__column a:hover {
        color: var(--lime);
      }

      .footer__bottom {
        min-height: 63px;

        display: grid;

        grid-template-columns:
          1fr
          1fr
          1fr;

        align-items: center;

        border-top:
          1px solid
          rgba(255,255,255,0.1);

        color:
          rgba(255,255,255,0.28);

        font-family: "DM Mono", monospace;

        font-size: 6px;

        letter-spacing: 0.06em;
      }

      .footer__bottom span:nth-child(2) {
        text-align: center;
      }

      .footer__bottom span:last-child {
        text-align: right;
      }


      /* =====================================================
         FLOATING REPORT
      ====================================================== */

      .floating-report {
        position: fixed;

        right: 21px;
        bottom: 20px;

        z-index: 400;

        min-height: 58px;

        padding:
          7px
          10px
          7px
          7px;

        display: inline-flex;

        align-items: center;

        gap: 9px;

        background: var(--lime);

        color: var(--ink);

        box-shadow:
          0 15px 30px
          rgba(0,0,0,0.14);

        transition:
          transform 160ms ease,
          box-shadow 160ms ease;
      }

      .floating-report:hover {
        transform: translateY(-3px);

        box-shadow:
          0 19px 35px
          rgba(0,0,0,0.17);
      }

      .floating-report__icon {
        width: 37px;
        height: 37px;

        display: grid;

        place-items: center;

        background: var(--ink);

        color: var(--lime);
      }

      .floating-report__text {
        display: grid;

        gap: 3px;
      }

      .floating-report__text small {
        color:
          rgba(19,23,25,0.52);

        font-family: "DM Mono", monospace;

        font-size: 5px;

        letter-spacing: 0.07em;
      }

      .floating-report__text strong {
        font-size: 10px;

        font-weight: 800;
      }


      /* =====================================================
         1100
      ====================================================== */

      @media (max-width: 1100px) {

        .desktop-nav {
          gap: 18px;
        }

        .hero__grid {
          grid-template-columns:
            minmax(0, 1fr)
            minmax(370px, 0.78fr);

          gap: 45px;
        }

        .modules__head,
        .method__header,
        .coverage__head,
        .material__head {
          gap: 50px;
        }

      }


      /* =====================================================
         860
      ====================================================== */

      @media (max-width: 860px) {

        .page-width {
          width:
            min(
              calc(100% - 34px),
              var(--page)
            );
        }

        .desktop-nav,
        .header__location,
        .header__action {
          display: none;
        }

        .header__right {
          margin-left: auto;
        }

        .menu-button {
          display: inline-flex;
        }

        .hero__grid {
          min-height: auto;

          grid-template-columns: 1fr;

          gap: 0;
        }

        .hero__main {
          padding:
            68px
            0
            55px;
        }

        .hero h1 {
          font-size: clamp(
            56px,
            10vw,
            84px
          );
        }

        .hero__interface {
          margin-bottom: 0;
        }

        .hero__signal {
          grid-template-columns:
            repeat(3, 1fr);
        }

        .hero__signal > div:nth-child(3) {
          border-right: 0;
        }

        .hero__signal > div:nth-child(-n + 3) {
          border-bottom:
            1px solid
            var(--line);
        }

        .statement__grid {
          grid-template-columns: 1fr;

          gap: 25px;
        }

        .statement__meta {
          flex-direction: row;

          align-items: center;

          gap: 9px;
        }

        .modules__head,
        .method__header,
        .coverage__head,
        .material__head {
          grid-template-columns: 1fr;

          gap: 28px;
        }

        .module-grid {
          grid-template-columns: 1fr;
        }

        .dark-section__headline {
          padding-top: 58px;
        }

        .method__grid {
          grid-template-columns: 1fr;
        }

        .method-card {
          min-height: 230px;

          border-right: 0;

          border-bottom:
            1px solid
            var(--line);
        }

        .method-card:last-child {
          border-bottom: 0;
        }

        .coverage__body {
          grid-template-columns: 1fr;
        }

        .coverage__map {
          min-height: 390px;
        }

        .about__header {
          align-items: flex-start;

          flex-direction: column;
        }

        .about__grid {
          grid-template-columns: 1fr;
        }

        .footer__grid {
          grid-template-columns:
            1fr
            1fr;
        }

        .footer__identity {
          grid-column: 1 / -1;
        }

        .final__main {
          align-items: flex-start;

          flex-direction: column;
        }

      }


      /* =====================================================
         620
      ====================================================== */

      @media (max-width: 620px) {

        .page-width {
          width:
            calc(100% - 26px);
        }

        .hero__top span {
          font-size: 6px;
        }

        .hero__top div:last-child {
          display: none;
        }

        .hero__main {
          padding:
            54px
            0
            49px;
        }

        .hero h1 {
          font-size:
            47px;
        }

        .hero__intro {
          margin-top: 27px;

          font-size: 13px;
        }

        .hero__actions {
          width: 100%;

          flex-direction: column;
        }

        .button {
          width: 100%;
        }

        .interface__body {
          min-height: 340px;
        }

        .interface__shape--one {
          width: 205px;
          height: 255px;

          left: 30px;
        }

        .interface__shape--two {
          width: 135px;
          height: 170px;

          right: 15px;
        }

        .interface__center strong {
          font-size: 31px;
        }

        .hero__signal {
          grid-template-columns:
            repeat(2, 1fr);
        }

        .hero__signal > div:nth-child(2),
        .hero__signal > div:nth-child(4) {
          border-right: 0;
        }

        .hero__signal > div:nth-child(5) {
          grid-column: 1 / -1;

          border-top:
            1px solid
            var(--line);
        }

        .statement,
        .modules,
        .method,
        .coverage,
        .material,
        .about {
          padding: 82px 0;
        }

        .statement h2,
        .modules__head h2,
        .method__header h2,
        .coverage__head h2,
        .material__head h2,
        .about__header h2 {
          font-size: 41px;
        }

        .statement__lower {
          grid-template-columns: 1fr;

          gap: 18px;
        }

        .module-card {
          min-height: 315px;
        }

        .dark-section {
          padding: 82px 0;
        }

        .dark-section__label {
          display: none;
        }

        .dark-section__headline h2 {
          font-size: 43px;
        }

        .dark-section__bottom {
          align-items: flex-start;

          flex-direction: column;
        }

        .method__grid {
          margin-top: 54px;
        }

        .method-card {
          grid-template-columns:
            38px
            45px
            minmax(0, 1fr);
        }

        .method-card__body {
          padding-right: 10px;
        }

        .coverage__map {
          min-height: 290px;
        }

        .coverage__outline--one {
          width: 200px;
          height: 230px;

          left: 45px;
        }

        .coverage__outline--two {
          width: 130px;
          height: 155px;

          right: 25px;
        }

        .coverage__outline--three {
          width: 110px;
          height: 120px;

          left: 130px;
        }

        .coverage__center strong {
          font-size: 29px;
        }

        .coverage-row {
          min-height: 128px;

          grid-template-columns:
            34px
            minmax(0, 1fr)
            80px
            19px;

          gap: 10px;
        }

        .coverage-row__name strong {
          font-size: 19px;
        }

        .coverage-row__status {
          font-size: 5px;
        }

        .material-row {
          min-height: 88px;

          grid-template-columns:
            35px
            44px
            minmax(0, 1fr)
            20px;

          gap: 10px;
        }

        .material-row__name strong {
          font-size: 17px;
        }

        .material-row__name p {
          display: none;
        }

        .about__statement {
          min-height: 310px;
        }

        .principle {
          min-height: 128px;

          grid-template-columns:
            35px
            minmax(0, 1fr)
            16px;

          gap: 12px;
        }

        .final__inner {
          min-height: 410px;
        }

        .final__top span:last-child {
          display: none;
        }

        .final__main h2 {
          font-size: 53px;
        }

        .final__button {
          width: 100%;
        }

        .footer__grid {
          grid-template-columns:
            1fr
            1fr;

          gap: 40px;
        }

        .footer__bottom {
          min-height: auto;

          grid-template-columns: 1fr;

          gap: 9px;

          padding:
            18px
            0
            95px;
        }

        .footer__bottom span:nth-child(2),
        .footer__bottom span:last-child {
          text-align: left;
        }

        .floating-report {
          left: 11px;
          right: 11px;
          bottom: 11px;
        }

        .floating-report__text {
          flex: 1;
        }

      }


      /* =====================================================
         420
      ====================================================== */

      @media (max-width: 420px) {

        .hero h1 {
          font-size: 43px;
        }

        .interface__body {
          min-height: 280px;
        }

        .interface__center strong {
          font-size: 27px;
        }

        .statement h2,
        .modules__head h2,
        .method__header h2,
        .coverage__head h2,
        .material__head h2,
        .about__header h2 {
          font-size: 36px;
        }

        .dark-section__headline h2 {
          font-size: 38px;
        }

        .final__main h2 {
          font-size: 47px;
        }

      }
    </style>
  </body>
</html>
